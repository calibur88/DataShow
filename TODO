# DSQL / DataShow v2.0 升级 TODO（最终版）

> **版本**：2.0.0（破坏性大版本）  
> **核心决策**：`TABLE_VIEW` / `LIST_VIEW` / `CARD_VIEW` 三者平等，均可写入 SQL 并持久化；旧 `TABLE` / `LIST` 直接报语法错误，不做任何兼容

---

## 破坏性变更清单

| 旧 | 新 |
|---|---|
| `TABLE` | `TABLE_VIEW` |
| `LIST` | `LIST_VIEW` |
| `'table'` / `'list'` / `'board'` | `'TABLE_VIEW'` / `'LIST_VIEW'` / `'CARD_VIEW'` |

- 旧关键词直接报语法错误，不做任何兼容处理
- 三个 `*_VIEW` 关键词全部允许写进 SQL，并持久化到 `data.json`
- 执行层：`CARD_VIEW` 与 `TABLE_VIEW` 走同一条数据管道，仅渲染不同
- 视图切换与 SQL 开头关键词双向同步

---

## 阶段零：语言层变更（解析器）

- [ ] **0.1 词法分析器 (`src/query/lexer.ts`)**  
  删除 `TABLE` / `LIST` token，新增 `TABLE_VIEW` / `LIST_VIEW` / `CARD_VIEW` token，更新关键字映射表。

- [ ] **0.2 语法分析器 (`src/query/parser.ts`)**  
  修改 `view` 产生式为 `TABLE_VIEW | LIST_VIEW | CARD_VIEW`（缺省 `TABLE_VIEW`）。  
  遇到旧 `TABLE` / `LIST` 直接报语法错误。

- [ ] **0.3 执行器 (`src/query/executor.ts`)**  
  确认视图关键词不影响执行逻辑，三者走完全相同的数据管道。

---

## 阶段一：数据模型与存储

- [ ] **1.1 类型定义 (`src/types.ts`)**  
  定义 `ViewType = 'TABLE_VIEW' | 'LIST_VIEW' | 'CARD_VIEW'`，更新 `Board.type` 为该类型。

- [ ] **1.2 存储层 (`src/store.ts`)**  
  - `normalizeBoard`：白名单校验，未知类型回退 `'TABLE_VIEW'`
  - 加载时用 `detectTypeFromSql` 对齐 `board.type` 与 SQL 开头关键词
  - 无旧版兼容逻辑

---

## 阶段二：视图切换双向同步

- [ ] **2.1 工具函数 (`src/utils/viewSync.ts` — 新建)**  
  - `applyViewType(board, newType)`：替换/注入 SQL 开头的视图关键词
  - `detectTypeFromSql(sql)`：识别开头关键词，返回对应 `ViewType`
  - `normalizeSqlView(sql, type)`：强制 SQL 视图关键词与 `type` 对齐

- [ ] **2.2 下拉菜单切换**  
  监听下拉 `onChange` → 调用 `applyViewType` → 更新 store → 保存 `data.json` → 刷新面板。

- [ ] **2.3 SQL 编辑器反向同步**  
  防抖保存时调用 `detectTypeFromSql`，若与当前 `board.type` 不一致则自动切换下拉。

---

## 阶段三：视图组件渲染

- [ ] **3.1 拆分视图组件 (`src/ui/views/`)**  
  创建 `TableView.svelte`、`ListView.svelte`、`CardView.svelte`，根据 `board.type` 分发。

- [ ] **3.2 `TableView`（只读）**  
  标准表格，点击行跳转原文，**移除所有内联编辑**。

- [ ] **3.3 `ListView`（只读，参考 Obsidian Bases）**  
  - 列表项：文件名（点击跳转），额外属性作为缩进子信息
  - 项目符号 / 缩进属性（初期写死默认值，不进 `Board` 数据模型）
  - 初期只读

- [ ] **3.4 `CardView`（独占内联编辑）**  
  - 网格流式布局，每张卡片显示一个笔记的投影字段
  - 单击字段值进入编辑（文本/数字/布尔/数组），派生列只读
  - 保存调用 `fileManager.processFrontMatter` 原子写回，防抖

---

## 阶段四：测试

- [ ] **4.1 单元测试**  
  - `viewSync.test.ts`：`applyViewType`、`detectTypeFromSql`、`normalizeSqlView`
  - `normalizeBoard.test.ts`：白名单校验、未知回退
  - `lexer.test.ts` / `parser.test.ts`：新关键词识别、旧关键词报错

- [ ] **4.2 集成测试（手动）**  
  视图切换、SQL 反向同步、卡片编辑、旧关键词报错、各视图渲染。

---

## 阶段五：文档

- [ ] **5.1 `docs/DSQL-EBNF.md`（纯语言规范，无历史记录）**  
  仅保留 EBNF 语法 + 语义规则 + 当前版本号（v2.0），删除末尾所有历史版本记录。

- [ ] **5.2 `CHANGELOG.md`（合并插件 + DSQL 语言更新记录）**  
  结构：
  ```markdown
  # 更新日志

  ## 插件版本

  ### 2.0.0
  - 视图关键词升级：`TABLE` → `TABLE_VIEW`，`LIST` → `LIST_VIEW`，新增 `CARD_VIEW`
  - 旧关键词直接报语法错误
  - 新增卡片视图（内联编辑）
  - 表格/列表视图改为只读
  - 视图切换与 SQL 双向同步

  ### 1.x.x（现有内容保留）

  ## DSQL 语言版本

  ### v2.0（2026-09-08）
  - 视图关键词：`TABLE_VIEW` | `LIST_VIEW` | `CARD_VIEW`（缺省 `TABLE_VIEW`）
  - 废除 `TABLE` / `LIST`

  ### v1.5（2026-09-07）
  - 三值语义分家、别名唯一性、摄取容错

  ### v1.4（2026-09-07）
  - TOTAL 聚合、$变量$ 派生

  ### v1.3（2026-09-07）
  - 前件关系、SELECT 可省略

  ### v1.2（2026-09-07）
  - 标记语法字面化、表达式完备

  ### v1.1（2026-09-07）
  - SORT BY 自定义优先级
  ```

- [ ] **5.3 更新 `README.md`**  
  视图类型说明、卡片视图功能介绍。

---

## 阶段六：发布准备

- [ ] **6.1 版本号升级**  
  `manifest.json` → `2.0.0`，`versions.json` 新增 `"2.0.0": "1.4.4"`。

- [ ] **6.2 打包自测**  
  `npm run build` 无报错，在测试 vault 中完整演练所有功能。

---

## 文件清单

| 文件 | 操作 |
|------|------|
| `src/types.ts` | 修改 |
| `src/store.ts` | 修改 |
| `src/query/lexer.ts` | 修改 |
| `src/query/parser.ts` | 修改 |
| `src/query/executor.ts` | 检查 |
| `src/utils/viewSync.ts` | **新建** |
| `src/ui/components/Panel.svelte` | 修改 |
| `src/ui/views/TableView.svelte` | **新建** |
| `src/ui/views/ListView.svelte` | **新建** |
| `src/ui/views/CardView.svelte` | **新建** |
| `docs/DSQL-EBNF.md` | 修改（删除历史记录） |
| `CHANGELOG.md` | 修改（合并插件 + DSQL 语言历史） |
| `README.md` | 修改 |
| `manifest.json` | 修改 |
| `tests/viewSync.test.ts` | **新建** |
| `tests/normalizeBoard.test.ts` | **新建** |

---

## 实施优先级

- **P0**：阶段零 + 阶段一（基础设施）
- **P1**：阶段二（双向同步）
- **P2**：阶段三（视图组件，可并行）
- **P3**：阶段四 + 阶段五（测试与文档）
- **P4**：阶段六（发布）