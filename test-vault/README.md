# DataShow 功能展示 vault

> 本 vault 是**干净提交库**，只用于版本提交；日常在 Obsidian 中验收请用 `../test-vault-local/`
> （两者预置看板一致，本地测试产生的改动不会影响本 vault）。
>
> **DSQL v2.0 标记语法**：关键词用 `**` 包裹（如 `**SELECT**`）、运算符用 `%` 包裹（如 `%==%`）、
> 字符串用单引号 `'值'`、路径用双引号 `"文件夹"`。视图关键词为 `**TABLE_VIEW**` /
> `**LIST_VIEW**` / `**CARD_VIEW**`（旧 `**TABLE**` / `**LIST**` 已废除）。完整规范见
> [`../docs/DSQL-语言规范.md`](../docs/DSQL-语言规范.md)。

## 示例数据（示例/）

| 文件夹 | 内容 |
|---|---|
| `示例/功能示例/` | 任务A/B/C：status/owner/priority/due/blocked 字段 + 互链（属性编辑、反查链接演示） |
| `示例/数学示例/` | 订单一/二/三：价格、数量、运费、底数、指数、数值（数学运算与函数演示） |
| `示例/DSQL语言示例/` | 语法演示A/B：中文标识符、数组字段、大小写敏感标签、缺失字段（语法特性演示） |
| `示例/三值示例/` | 正常零值/空容器/未赋值：演示 DSQL 1.5 三种「无」的语义正交分家 |

## 预置看板（四大类）

已预置 18 个看板，按侧栏「类型」分为四大类，与单测三大类 + 三值示例演示一一对应
（**DSQL语言示例 = DSQL 语法规范演示**，仅在看板面板中查询；不支持、也不计划支持 ```datashow 代码块）：

- **功能示例**（6 个）：五子句查询、SORT BY 状态机排序、反查链接列表、布尔与日期、全字段自动列、任务卡片（CARD_VIEW）；
- **数学示例**（7 个）：四则与连接、乘方与取模（含除零 null 演示）、内置函数、比较与逻辑、汇总行（TOTAL 计数求和 + 派生平均）、占比（TOTAL 恒忽略 WHERE）、数学卡片（CARD_VIEW）；
- **DSQL语言示例**（5 个，语法规范演示；聚合语法 `**TOTAL**` / `$变量$` 的演示放在数学示例类）：省略 SELECT、子句乱序、WITHOUT ID 任意位置、中文标识符与字段缺失、空 BY 警告；
- **三值示例**（2 个，DSQL 1.5 三值语义演示）：三值展示（三行全部输出，缺失字段显示为 —，验证「null/empty 列为空」渲染）、empty 谓词与三值传播（七列对照：empty() 各输入、empty 算术/连接/比较传播、零值照常运算）。

## 可复制示例（DSQL 1.5）

**汇总行（SELECT 仅含 TOTAL 项 → 单行结果）**

```sql
**TABLE** **SELECT**
  **TOTAL** 1 **AS** $笔记数$,
  **TOTAL** 价格 **AS** $总价格$,
  ($总价格$ %/% $笔记数$) **AS** $均价$
**FROM** "示例/数学示例"
```

**占比（TOTAL 恒忽略 WHERE：WHERE 只过滤显示行，聚合仍是全表口径）**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 名称,
  价格,
  **TOTAL** 价格 **AS** $全表总价格$,
  (价格 %/% $全表总价格$) %*% 100 **AS** $占比$
**FROM** "示例/数学示例"
**WHERE** 价格 %>=% 7
**SORT** 价格 **DESC**
```

**三值展示（DSQL 1.5：三种「无」互不混淆；缺失字段显示为 —）**

```sql
**SELECT** file.name **AS** 名称, 数量, 价格, 开关
**FROM** "示例/三值示例"
**SORT** 价格 **DESC**
```

三行全部输出：`数量` 在正常零值行为 `0`、另两行因字段缺失显示为 `—`；
**注意**：若改用 `**WHERE** 数量` 过滤，三行都会被剔除（0 / null / null 在裸真值判断中均为假）。

**empty 谓词与三值传播**

```sql
**SELECT** file.name **AS** 名称,
  **empty**(未填) **AS** 未填是empty,
  **empty**(备注) **AS** 空串是empty,
  未填 %+% 1 **AS** empty算术
**FROM** "示例/三值示例"
**SORT** 价格 **DESC**
```

`**empty**()` 仅认「未赋值」；`""` / `[]` / `0` / `false` / null / 缺失字段均为 false。

## 操作

1. 设置 → DataShow → 看板：查看/增删看板（名称/分类/说明 + 视图模式）；
2. 左侧栏 DataShow 图标打开看板侧栏（按分类分组），点击看板在主工作区打开面板；
3. 面板 DSQL 编辑框可直接改查询（防抖自动保存），「刷新」手动重跑，「视图」下拉切换
   跟随语句 / 表格 / 列表 / 卡片（强制覆盖时会同步 SQL 开头关键词）；
4. **卡片视图**单击字段值直接编辑 frontmatter（派生列只读），保存后结果自动刷新；
   表格 / 列表为只读，点击行或文件名打开笔记；
5. 「查询结果 / 调试信息」标签页展示 FROM/WHERE/SORT/LIMIT 各阶段统计与 warnings（设置可关）。

## 开发

```bash
cd ..        # 回到项目根目录
npm run build   # 产出 dist/main.js，并自动同步到本 vault 与 test-vault-local 的
                # .obsidian/plugins/data-show/
npm test        # 单测七套件：功能 / 数学 / DSQL语言 / store / 摄取层 / viewSync / normalizeBoard
```
