# DataShow 功能展示 vault

> 本 vault 是**干净提交库**，只用于版本提交；日常在 Obsidian 中验收请用 `../test-vault-local/`
> （两者预置看板一致，本地测试产生的改动不会影响本 vault）。
>
> **DSQL v2.0 标记语法**：关键词用 `**` 包裹（如 `**SELECT**`）、运算符用 `%` 包裹（如 `%==%`）、
> 字符串用单引号 `'值'`、路径用双引号 `"文件夹"`。视图关键词为 `**TABLE_VIEW**` /
> `**LIST_VIEW**` / `**CARD_VIEW**`（旧 `**TABLE**` / `**LIST**` 已废除）。完整规范见
> [`../docs/DSQL-语言规范.md`](../docs/DSQL-语言规范.md)。

## 示例数据（示例/）

| 文件夹 | 内容 |
|---|---|
| `示例/功能示例/` | 任务A/B/C：status/owner/priority/due/blocked 字段 + 互链（属性编辑、反查链接演示） |
| `示例/数学示例/` | 订单一/二/三：价格、数量、运费、底数、指数、数值（数学运算与函数演示） |
| `示例/DSQL语言示例/` | 语法演示A/B：中文标识符、数组字段、大小写敏感标签、缺失字段（语法特性演示） |
| `示例/三值示例/` | 正常零值/空容器/未赋值：演示 DSQL 1.5 三种「无」的语义正交分家 |
| `示例/人物关系示例/` | 张三/李四/王五/赵六/孙七：姓名、性别、年龄、阵营、身份、状态 + 互相 wiki 链接（**跨文件关系查询**与 **AND / OR / NOT 逻辑判断**演示）；`关系` frontmatter 数组作为数组查询备用示例 |
| `示例/场景示例/` | 长坂坡/赤壁/洛阳商行/幽篁大营：类型、地点、季节、结果 + `场景人物` 数组 + 指向人物的 wiki 链接（**跨文件夹**：场景 ↔ 人物 双向查询演示） |

## 预置看板（六大类）

已预置 34 个看板，按侧栏「类型」分为六大类，与单测三大类 + 三值示例 + 跨文件示例 + 跨文件夹示例演示一一对应
（**DSQL语言示例 = DSQL 语法规范演示**，仅在看板面板中查询；不支持、也不计划支持 ```datashow 代码块）：

- **功能示例**（6 个）：五子句查询、SORT BY 状态机排序、反查链接列表、布尔与日期、全字段自动列、任务卡片（CARD_VIEW）；
- **数学示例**（7 个）：四则与连接、乘方与取模（含除零 null 演示）、内置函数、比较与逻辑、汇总行（TOTAL 计数求和 + 派生平均）、占比（TOTAL 恒忽略 WHERE）、数学卡片（CARD_VIEW）；
- **DSQL语言示例**（5 个，语法规范演示；聚合语法 `**TOTAL**` / `$变量$` 的演示放在数学示例类）：省略 SELECT、子句乱序、WITHOUT ID 任意位置、中文标识符与字段缺失、空 BY 警告；
- **三值示例**（2 个，DSQL 1.5 三值语义演示）：三值展示（三行全部输出，缺失字段显示为 —，验证「null/empty 列为空」渲染）、empty 谓词与三值传播（七列对照：empty() 各输入、empty 算术/连接/比较传播、零值照常运算）；
- **跨文件示例**（7 个，跨文件关系 + 逻辑判断演示）：人物出链（谁引用了张三）、人物入链反查（张三的关系圈）、AND 逻辑判断、OR 与优先级（AND 优先于 OR）、括号覆盖优先级、跨文件 + NOT 组合、人物阵营卡片（CARD_VIEW）；
- **跨文件夹示例**（7 个，`示例/场景示例/` ↔ `示例/人物关系示例/` 跨文件夹查询）：场景检索（含张三的场景）、场景人物清单、人物被某场景引用（入链跨文件夹）、人物跨场景出场（OR 组合）、场景 + 逻辑（AND / NOT）、出场人次汇总（TOTAL）、场景卡片（CARD_VIEW）。

两个关系文件夹的对照（`示例/人物关系示例/` 的 5 篇人物 + `示例/场景示例/` 的 4 篇场景）：

| 场景 | 类型 / 地点 | 场景人物 | 反向：谁被该场景引用 |
|---|---|---|---|
| 长坂坡 | 遭遇战 / 荆州 | 张三、王五 | 张三、王五 |
| 赤壁 | 水战 / 江夏 | 张三、李四、孙七 | 张三、李四、孙七 |
| 洛阳商行 | 密谈 / 洛阳 | 孙七、赵六 | 孙七、赵六 |
| 幽篁大营 | 军议 / 幽篁 | 王五、赵六 | 王五、赵六 |

## 可复制示例（跨文件关系与逻辑判断）

**跨文件正向关系：谁链接了「张三」（`file.outlinks`）**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 人物,
  阵营,
  身份,
  **length**(file.outlinks) **AS** 出链数
**FROM** "示例/人物关系示例"
**WHERE** **contains**(file.outlinks, "示例/人物关系示例/张三.md")
**SORT** 阵营 **ASC**
```

命中：李四、王五、孙七。把 `file.outlinks` 换成 `file.inlinks` 即为**反向反查**（谁链接了张三）。

**跨文件 + 逻辑取反：出链命中「王五」且未阵亡**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 人物,
  阵营,
  身份,
  **length**(file.outlinks) **AS** 出链数
**FROM** "示例/人物关系示例"
**WHERE** **contains**(file.outlinks, "示例/人物关系示例/王五.md") **AND** **NOT** (状态 %==% '阵亡')
**SORT** 阵营 **ASC**
```

命中：张三、赵六、孙七（王五本人未引用自己，且已阵亡）。

**AND 优先于 OR，括号可覆盖**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 人物, 阵营, 身份, 年龄
**FROM** "示例/人物关系示例"
**WHERE** 阵营 %==% '中立' **OR** 阵营 %==% '天枢' **AND** 身份 %==% '谋士'
**SORT** 阵营 **ASC**
```

命中：孙七（中立）、李四（天枢谋士）——等价于 `中立 OR (天枢 AND 谋士)`；
写成 `(阵营 %==% '天枢' **OR** 阵营 %==% '幽篁') **AND** 状态 %==% '存活'` 则命中张三、李四、赵六。

**跨文件夹：场景侧反查人物（`示例/场景示例/` → 人物）**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 场景,
  类型,
  地点,
  **length**(场景人物) **AS** 出场人数
**FROM** "示例/场景示例"
**WHERE** **contains**(场景人物, '张三')
**SORT** 地点 **ASC**
```

命中：赤壁、长坂坡（`场景人物` 是 frontmatter 数组，`**contains**` 对数组做严格相等匹配）。

**跨文件夹：人物侧反查场景（人物 ← `示例/场景示例/`）**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 人物,
  阵营,
  身份,
  **length**(file.inlinks) **AS** 被引用次数
**FROM** "示例/人物关系示例"
**WHERE** **contains**(file.inlinks, "示例/场景示例/长坂坡.md")
**SORT** 阵营 **ASC**
```

命中：张三、王五。查「同时出现在长坂坡或幽篁大营」只需把两个 `**contains**` 用 `**OR**` 连起来
（命中张三、王五、赵六）。

**场景 + 逻辑取反：含张三且结果不是僵持**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 场景, 类型, 地点, 结果
**FROM** "示例/场景示例"
**WHERE** **contains**(场景人物, '张三') **AND** **NOT** (结果 %==% '僵持')
**SORT** 地点 **ASC**
```

命中：长坂坡（赤壁因结果为「僵持」被 `**NOT**` 剔除）。

## 可复制示例（DSQL 1.5）

**汇总行（SELECT 仅含 TOTAL 项 → 单行结果）**

```sql
**TABLE_VIEW** **SELECT**
  **TOTAL** 1 **AS** $笔记数$,
  **TOTAL** 价格 **AS** $总价格$,
  ($总价格$ %/% $笔记数$) **AS** $均价$
**FROM** "示例/数学示例"
```

**占比（TOTAL 恒忽略 WHERE：WHERE 只过滤显示行，聚合仍是全表口径）**

```sql
**TABLE_VIEW** **SELECT**
  file.name **AS** 名称,
  价格,
  **TOTAL** 价格 **AS** $全表总价格$,
  (价格 %/% $全表总价格$) %*% 100 **AS** $占比$
**FROM** "示例/数学示例"
**WHERE** 价格 %>=% 7
**SORT** 价格 **DESC**
```

**三值展示（DSQL 1.5：三种「无」互不混淆；缺失字段显示为 —）**

```sql
**SELECT** file.name **AS** 名称, 数量, 价格, 开关
**FROM** "示例/三值示例"
**SORT** 价格 **DESC**
```

三行全部输出：`数量` 在正常零值行为 `0`、另两行因字段缺失显示为 `—`；
**注意**：若改用 `**WHERE** 数量` 过滤，三行都会被剔除（0 / null / null 在裸真值判断中均为假）。

**empty 谓词与三值传播**

```sql
**SELECT** file.name **AS** 名称,
  **empty**(未填) **AS** 未填是empty,
  **empty**(备注) **AS** 空串是empty,
  未填 %+% 1 **AS** empty算术
**FROM** "示例/三值示例"
**SORT** 价格 **DESC**
```

`**empty**()` 仅认「未赋值」；`""` / `[]` / `0` / `false` / null / 缺失字段均为 false。

## 操作

1. 设置 → DataShow → 看板：查看/增删看板（名称/分类/说明 + 视图模式）；
2. 左侧栏 DataShow 图标打开看板侧栏（按分类分组），点击看板在主工作区打开面板；
3. 面板 DSQL 编辑框可直接改查询（防抖自动保存），「刷新」手动重跑，「视图」下拉切换
   跟随语句 / 表格 / 列表 / 卡片（强制覆盖时会同步 SQL 开头关键词）；
4. **卡片视图**单击字段值直接编辑 frontmatter（派生列只读），保存后结果自动刷新；
   表格 / 列表为只读，点击行或文件名打开笔记；
5. 「查询结果 / 调试信息」标签页展示 FROM/WHERE/SORT/LIMIT 各阶段统计与 warnings（设置可关）。

## 开发

```bash
cd ..        # 回到项目根目录
npm run build   # 产出 dist/main.js，并自动同步到本 vault 与 test-vault-local 的
                # .obsidian/plugins/data-show/
npm test        # 单测七套件：功能 / 数学 / DSQL语言 / store / 摄取层 / viewSync / normalizeBoard
```
