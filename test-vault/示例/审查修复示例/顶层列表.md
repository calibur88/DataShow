---
- 备选一: 甲
- 备选二: 乙
---
frontmatter 为顶层 YAML 序列（整个文档是列表，不是映射）。

- 修复前：两行列表项被重复键检测误判为顶层键 `- 备选一`，本篇被整体剔除并产出字段名错误的警告；
- 修复后（DSQL 2.3.1）：正常进入查询——frontmatter 非映射，仅 `file.*` 字段可用（其余列显示 —）。

配套看板：审查修复示例 → 修复·重复键剔除与顶层列表对照。
