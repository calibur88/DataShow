---
名称: 溢出与一元
超大: 1e308
微小: 1e-308
数值: 5
数字串: "5"
---
用于「修复·溢出守卫与一元正负号」看板（DSQL 2.3.1）：

- `超大 %/% 微小`、`超大 %*% 超大`：修复前显示 `Infinity`，修复后为 —（null，开调试可见非有限数警告）；
- `%-%数字串` 为 —（一元正负号仅作用于数值，不计警告）；`%-%数值` 为 -5（数值照常）；
- 对照：`数字串 %+% 0` 为 5——二元算术按 §6.3 ③ 隐式转换，数字串可转。
