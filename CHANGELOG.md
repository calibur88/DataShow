# DataShow 更新日志

> 版本按迭代顺序倒序排列，每个版本条目固定分为「插件更新」与「DSQL 更新」两类；
> 编写规范见 [CONTRIBUTING.md](CONTRIBUTING.md) §6。

## [2.1.2] - 2026-09-08（当前）

### 插件更新

**侧栏分类折叠**：看板侧栏的分类分组头可点击折叠 / 展开，管理看板较多时清单更清爽。

- 点击分组头切换折叠状态，折叠时隐藏该分类下的看板列表，caret 由 ▾ 旋转为 ▸（带过渡动画）；
- 分组头改为 pointer 光标并加 hover 变色，附 `aria-expanded` 无障碍标注；
- 折叠状态持久化到 `data.json`（`collapsedGroups` 字段，按分类名记录），
  重启 Obsidian 后保留；看板增删触发重渲染时折叠状态不丢失；
- 实现仅涉及 sidebar / types / styles，旧 `data.json` 无需迁移（缺失字段自动补默认值）；
- 验收：`tsc --noEmit` 零错误，106 例单元测试全部通过。

**工具条自适应布局**：面板工具条按面板宽度自适应排布，修复手机上工具条撑破面板导致整页横向滚动的问题。

- 搜索控件（输入框 + 搜索 + 清空）独立为 `datashow-toolbar__search-group` 分组；
- 桌面宽屏：搜索组靠左，弹性 spacer 把「刷新 + 视图选择器」推到右侧成一组；
- 窄屏（面板宽度 ≤520px，手机 / 桌面窄分栏，CSS 容器查询 `@container` 触发）：
  换为两行布局——第一行右对齐「刷新 + 视图」紧贴 DSQL 输入框下方，
  第二行搜索组占满整行（输入框自动拉伸），「查询结果 / 调试信息」与结果区随文档流自然下移；
- 面板标题行窄屏下允许换行，长标题不再横向溢出；
- 采用容器查询而非媒体查询：按面板自身宽度触发，桌面窄分栏与手机全屏一套规则通吃；
- 实现仅涉及 panel / styles，测试全部通过。

### DSQL 更新

本次无 DSQL 语法 / 语义变更。

- **版本兼容声明**：本版本功能逻辑与 2.1.1 完全兼容。

## [2.1.1] - 2026-09-08

### 插件更新

**卡片视图搜索控件**：面板工具条新增搜索功能，仅对卡片视图（CARD_VIEW）生效。

- 工具条布局调整为 `[刷新] [🔍 输入框] [搜索] [清空] [spacer] [视图▼]`；
- 搜索匹配：每张卡片的全部可见文本（标题 + 字段名 + 字段值），
  不区分大小写的子串匹配，隐藏不匹配卡片；
- 交互：输入关键词后点「搜索」或按 Enter 触发过滤；点「清空」恢复全部卡片；
- 空结果提示「没有匹配的卡片」，清空后消失；
- 视图联动：卡片视图下搜索控件启用；表格 / 列表视图下禁用（灰显不可交互）并清空搜索词；
- 搜索词仅会话内保留（不持久化到 data.json）：
  切换看板、切到非卡片视图时自动清空；
  store 自动刷新 / 编辑器失焦重跑时过滤自动重新套用，不丢词；
- 实现方式为 DOM 后置过滤，视图层（card-view / table-view / list-view）零改动，
  不影响卡片内联编辑、视图切换、防抖保存等既有功能。

**工程结构重构**：`src/` 迁移为 `dsql / index / ui` 三层目录，功能逻辑零变更。

- `src/query/` → `src/dsql/`（DSQL 语言层，`src/types.ts` 一并移入），保持零 Obsidian 依赖；
- `src/views/` → `src/ui/`（panel / sidebar 移至 ui/ 根，三视图与属性弹窗移至 `ui/views/`），
  `src/utils/viewSync.ts` → `src/ui/utils/viewSync.ts`；`main.ts` / `settings.ts` 留根；
- 跨层引用统一走路径别名 `@dsql/*` / `@index/*` / `@ui/*`
  （tsconfig / esbuild / 测试运行器三处同构配置）；
- 按注释规范重写全部源码注释（文件头 `@module` + `@description`，导出函数 `@param` + `@returns`），
  规范落稿 CONTRIBUTING.md §8；
- 代码清理（删除未使用 import）与文档同步（CONTRIBUTING / API / ARCHITECTURE / README）；
- 验收：`tsc --noEmit` 零错误，106 个单元测试全部通过。

### DSQL 更新

本次无 DSQL 语法 / 语义变更。

- **版本兼容声明**：本版本功能逻辑与 2.1.0 完全兼容。

---

## [示例文件夹更新] - 2026-09-08

### 示例文件夹

**新增跨文件人物关系示例**：补齐跨文件关系与逻辑判断的演示数据。

- 新增 `示例/人物关系示例/`：张三 / 李四 / 王五 / 赵六 / 孙七 五篇人物笔记，
  frontmatter 含 姓名 / 性别 / 年龄 / 阵营 / 身份 / 状态 / **`关系`** 数组，
  正文互相 wiki 链接（`[[...]]`）；
  张三 → 李四、王五、孙七；李四 → 张三、赵六；王五 → 张三；赵六 → 王五、李四；孙七 → 张三、王五；
- 演示库 `test-vault/` 与本地测试库 `test-vault-local/` 同步更新：示例笔记、
  `README.md` 与 `data.json` 看板定义三处一并落地。

**新增「跨文件示例」看板 7 个**（侧栏新分类，看板总数 20 → 27）：

- **人物出链（谁引用了张三）**：`**contains**(file.outlinks, "示例/人物关系示例/张三.md")` → 李四、王五、孙七；
- **人物入链反查（张三的关系圈）**：换用 `file.inlinks` 做反向查询 → 李四、王五、孙七；
- **AND 逻辑判断**：`阵营 %==% '天枢' **AND** 状态 %==% '存活'` → 张三、李四；
- **OR 与优先级（AND 优先于 OR）**：`中立 **OR** 天枢 **AND** 谋士` → 孙七、李四
  （等价于 `中立 OR (天枢 AND 谋士)`）；
- **括号覆盖优先级**：`(天枢 **OR** 幽篁) **AND** 存活` → 张三、李四、赵六（王五已阵亡被剔除）；
- **跨文件 + NOT 组合**：出链命中「王五」`**AND** **NOT**` 阵亡 → 张三、赵六、孙七；
- **人物阵营卡片（CARD_VIEW）**：按「阵营」分列的人物卡，可顺带验收卡片内联编辑 frontmatter。

**新增跨文件夹示例（场景 ↔ 人物）**：两个文件夹之间的双向关系查询。

- 新增 `示例/场景示例/`：长坂坡、赤壁、洛阳商行、幽篁大营 四篇场景笔记，
  frontmatter 含 `场景人物`（数组）/ 类型 / 地点 / 季节 / 结果，正文 wiki 链接到
  `示例/人物关系示例/` 下的人物笔记；
- 与已有的人物文件夹构成跨文件夹关系：场景侧用 `**contains**(场景人物, '张三')` 反查人物，
  人物侧用 `**contains**(file.inlinks, "示例/场景示例/长坂坡.md")` 反查场景。

**新增「跨文件夹示例」看板 7 个**（侧栏新分类，看板总数 27 → 34）：

- **场景检索（含张三的场景）**：`**contains**(场景人物, '张三')` → 赤壁、长坂坡；
- **场景人物清单（跨文件夹引用）**：`场景人物` 数组与 `**length**(file.outlinks)` 双口径对照 → 4 篇场景；
- **人物被某场景引用（入链跨文件夹）**：`file.inlinks` 命中「长坂坡」→ 张三、王五；
- **人物跨场景出场（OR 组合）**：入链命中「长坂坡」`**OR**`「幽篁大营」→ 张三、王五、赵六；
- **场景 + 逻辑（AND / NOT）**：含张三 `**AND** **NOT**` 结果为僵持 → 长坂坡（赤壁被剔除）；
- **出场人次汇总（TOTAL）**：`**length**(场景人物)` 与 `**TOTAL** 1 **AS** $场景数$` 并列 → 4 行；
- **场景卡片（CARD_VIEW）**：按「类型」分列的场景卡 → 4 张。

**演示库 README 同步**：示例数据表新增「人物关系示例」「场景示例」两行与两文件夹对照表；
预置看板 20 → 34、四大类 → 六大类；「可复制示例」段落补齐跨文件与跨文件夹两组示例。

- 测试：14 条新看板 SQL 全部通过 `parseQuery` 解析校验；用含 `关系` / `场景人物` 数组 + outlinks / inlinks
  的模拟行跑通全部 14 条查询，命中结果与上述预期逐条一致。
- **版本兼容声明**：本项仅新增演示数据与看板，不涉及插件功能与 DSQL 语法变更。

---

## [2.1.0] - 2026-09-07

> **项目架构重构（单目录工程化）**

### 插件更新

**工程结构优化**：工程由「正式版 + 开发版」双目录合并为**单目录工程**。

- 源码、测试、脚本、文档与演示库统一放在根目录，插件 ID 统一为 `data-show`。

**构建流程统一**：构建入口、产物路径与同步目标统一。

- `esbuild.config.mjs` 以 `src/main.ts` 为入口、产出 `dist/main.js`；
- 构建完成后自动把 `main.js` / `manifest.json` / `styles.css` 同步到
  `test-vault-local/`（本地测试库）与 `test-vault/`（干净提交库）；
- watch 模式额外监听 `manifest.json` 与 `styles.css` 的变化并自动重建同步。

**测试流程统一**：测试与类型检查的基准路径统一到项目根目录。

- `scripts/test.mjs` 修正为以项目根目录为基准，动态加载 `tests/all.ts` 并执行全部单测；
- `tests/helpers.ts` 的测试库路径常量固定指向 `test-vault-local`；
- `tsconfig.json` 的 `outDir` / `rootDir` 分别指向 `dist` / `src`。

**演示库拆分**：本地验收环境与提交版本分离。

- 本地验收用 `test-vault-local/`（不入库，测试改动不影响提交版本），提交用干净版本 `test-vault/`；
- 两者 `.gitignore` 均排除构建产物、保留 `data.json` 与示例笔记。

**代码清理**：移除冗余导入与残留旧件。

- 删除编译期可证明未使用的导入；
- 统一共享类型出口为 `src/types.ts`；
- 清理测试库中残留的旧开发插件目录（`datashow-dev`），社区插件清单与看板 `viewType`
  统一到 `data-show` / DSQL v2.0 取值。

**文档统一**：按单目录工程重写全部文档。

- 重写 `README.md` / `ARCHITECTURE.md` / `CONTRIBUTING.md` / `API.md`；
- DSQL 规范整理为 `docs/DSQL-语言规范.md`。

**版本与兼容性**：三处版本号统一为 `2.1.0`。

- `manifest.json`、`package.json`、`versions.json` 三处版本号统一为 `2.1.0`；
- 已有 `data.json` 看板定义可直接沿用，无需迁移。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v2.0，权威规范见
[docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)。

- 对已有查询的影响：**兼容**，任何看板 SQL 都无需改动；
- 本版本不涉及 DSQL 语法变更，也不涉及看板视图与界面交互变更。

- 测试：106 例（七套件）全部通过；`npm run build` 通过。
- **版本兼容声明**：本版本功能逻辑与 2.0.x 完全兼容。

---

## [2.0.0] - 2026-09-07

> DSQL v2.0 + 卡片视图 + 字段迁移。
>
> **破坏性大版本**：DSQL 关键词 `TABLE` / `LIST` 直接报语法错误；看板定义 `viewOverride` 字段
> 在加载时迁移到 `viewType`（重命名，语义更明确）。演示库看板已全部同步更新。

### 插件更新

**新增卡片视图（CARD_VIEW）**：Kanban 看板布局。

- 按首个可分组字段（如「状态」）拆列，列头显示彩色圆点 + 分组值 + 数量，列内堆卡片；
- 卡片以文件名作标题（点击打开笔记），**单击字段值直接编辑 frontmatter**
  （回车保存 / Esc 取消 / 失焦还原）；
- 派生列（TOTAL / 表达式 / 变量 / `file.*` / `this.*`）只读；
- 保存走 `app.fileManager.processFrontMatter` 原子写回，索引增量更新自动重跑结果。

**表格 / 列表视图改为只读**：内联编辑统一改为打开笔记。

- 表格移除原双击单元格内联编辑，改为行级 click 打开笔记；
- 列表改为点击文件名 / 行打开笔记，派生列作为缩进子信息（参考 Obsidian Bases 风格）。

**视图切换与 SQL 双向同步**（`src/utils/viewSync.ts`）：下拉切换与 SQL 编辑互为反向同步。

- `applyViewType(board, newType)`：替换 / 注入 SQL 开头视图关键词 + 设置 `board.viewType`；
- `detectTypeFromSql(sql)`：跳过前导空白与 `--` 注释行识别开头 VIEW 关键词（未命中返回 `null`）；
- `normalizeSqlView(sql, type)`：纯函数，强制 SQL 开头关键词与 type 对齐
  （旧 `**TABLE**` / `**LIST**` 一并替换）；
- 面板下拉切换 → 调 `applyViewType` → 保存 → 重渲染；SQL 编辑器防抖保存 → 调
  `detectTypeFromSql` 反向同步下拉选中态（空 SQL / 纯空白不切换，保持当前状态）。

**字段迁移**：`Board.viewOverride` → **`Board.viewType`**。

- 类型 `ViewType | ""`，空 = 跟随语句；
- 加载时按优先级迁移（`viewType` 合法值优先，非法 / 空时 `viewOverride` 兜底）并写回一次；
- `Board.type` 保持 `string` 不变——仍是用户自由填写的看板分类（侧栏分组依据）。

**设置页**：视图模式与看板分类并存。

- 新增「视图模式」下拉（跟随语句 / 表格 / 列表 / 卡片），与「看板分类」自由输入并存；
- 「看板类型」标签改为「看板分类」。

**演示库同步**：看板 18 → 20。

- 新增「任务卡片」「数学卡片」两个 CARD_VIEW 示例；
- 全部看板 SQL 已同步到 DSQL v2.0，`viewOverride` 字段已迁移为 `viewType`；
- 迁移前的看板定义留存为 `data.json.back` 快照，便于回滚比对。

### DSQL 更新

**DSQL 语言升级 v2.0**（规范见 [docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)）：视图关键词统一 `*_VIEW` 后缀。

- 视图关键词 `TABLE` / `LIST` 废除，新增 `TABLE_VIEW` / `LIST_VIEW` / `CARD_VIEW`
  （缺省 `TABLE_VIEW`）；旧 `**TABLE**` / `**LIST**` 直接抛 `LexError`「未知关键词」，不做兼容；**破坏性**
- 三个 `*_VIEW` 关键词全部允许写进 SQL 并持久化到看板定义；
- 执行层：三个视图走完全相同的数据管道，仅渲染不同；
- 字符串字面量 `'**TABLE_VIEW**'` 等不参与视图识别（词法器天然隔离）；
- 对已有查询的影响：**不兼容**——使用旧 `**TABLE**` / `**LIST**` 的查询直接报语法错误，
  须改为 `*_VIEW` 关键词。

- 测试：80 → 106 例（七套件，新增 26 例）；新增 `tests/viewSync.test.ts`（15 例）与
  `tests/normalizeBoard.test.ts`（10 例），`tests/dsql-language.test.ts` 更新 view 字面量
  并新增旧关键词报错与字符串字面量边界用例；`npm run build` 通过。

---

## [1.8.001] - 2026-09-07（插件开发版，已随 1.8.0 迁移）

> DSQL 1.5：三值语义分家 / 别名唯一 / 摄取容错。
>
> 本条目内容已随插件 **1.8.0** 迁移到正式版（插件版本不变，仅 DSQL 语言版本升 v1.5）；
> 演示库的「三值示例」分组与 `data.json` 入库同步记入 1.8.0。

### 插件更新

**调试信息结构化渲染**：warnings 带类型展示。

- warnings 结构化 `{ type, message }`（除零 / 类型不匹配 / 未知函数 / TOTAL / SORT /
  duplicateKey 等），调试页渲染为 `[type] message`。

**测试**：新增摄取层套件。

- 新增 `ingest.test.ts`（重复键 / 摄取归一 4 例）与三值语义、别名唯一性、词法等新例，
  warnings 断言适配结构化，共 **80 例通过**。

### DSQL 更新

**规范升 v1.5**：三处「无」的语义正交分家。

- `0` / `false` 为正常值（仅裸真值为假，`0 %+% 1 = 1` 照常）；
- `null` 为空容器值（摄取归一 `字段: ""` / `字段: []` → null；算术 → null + warning，
  比较 → false）；
- **empty 值**为未赋值（`字段:` 冒号后无内容；除 `empty()` 外一切运算按 null 传播）；
- 实现：EMPTY 哨兵 + 求值层 empty → null 传播，`empty()` 是唯一能看见 empty 值的运算。

**裸真值判断**：假值集合扩大。

- empty 值、null、0、false、空串、空数组均为假（**0 由真改假**）；**破坏性**

**`empty()` 语义收窄**：当且仅当 x 为 empty 值时 true。

- `""` / `[]` / `0` / `false` / null / 缺失字段均 false。

**NUMBER 词法收窄**：小数点后必须至少一位数字。

- `1.` / `.5` 词法报错。

**别名唯一性**（致命错误，聚合遍开始前）：命名空间隔离。

- SELECT 内所有 AS 别名互不相同（解析期校验）；
- 且不得与 FROM 全量命中行字段名并集重复（非 TOTAL 场景同样生效）。**破坏性**

**frontmatter 摄取容错**：重复键隔离而非中断。

- 重复键（自扫描原文顶层键，Obsidian metadataCache 已折叠无此信息）
  → 文件从结果集剔除 + 结构化 warnings `duplicateKey`（含文件名与字段名，原始键值对归档），
  查询继续；
- 受限标注：`字段:` 与 `字段: null` 在缓存层不可区分，
  实现按「键存在但值为 null → empty 值」近似（`字段: null` / `~` 亦映射为 empty）。

**规范文档整理**：小节编号与关键词清单勘误。

- §6 小节编号勘误 5.1–5.8 → 6.1–6.8；删除版本记录重复引言行，「当前」仅标 v1.5；
- §1 关键词清单补 `TOTAL`；新增「摄取容错策略」骨架节。

- 对已有查询的影响：**部分不兼容**——同名别名、依赖 `0` 为真的判断需按新语义调整。

---

## [1.8.0] - 2026-09-07

### 插件更新

**DSQL 1.4 迁移合入**：自测试版 1.7.003 合入 TOTAL 全表聚合与 `$变量$` 派生体系。

- 语言规范升 v1.4，两目录测试与文档版本对齐（详见下方 DSQL 更新）。

**演示库同步**：新增三值示例分组。

- 新增 `示例/三值示例/`（正常零值 / 空容器 / 未赋值 三篇）；
- 预置看板 16 → 18（新增「三值示例」分组：三值展示、empty 谓词与三值传播）；
- 看板定义 `data.json` 已纳入版本控制（含全部 DSQL，视作核心资产）；
- 修复潜在不兼容：原 m4-compare 看板使用「价格 `**AS**` 价格」同名别名，
  在 1.5 别名唯一性规则下会致命报错，已改为「物品价格 / 物品数量」。

**测试补全**：字符串连接与复合表达式 8 例。

- 覆盖字符串连接（null 传播 / 类型转换 / 优先级 / 参与比较）与
  复合表达式（优先级括号 / 逻辑组合 / 函数嵌套）。

**代码清理与文档核对**：删除死代码并统一文档表述。

- 删除死代码 `isEmptyValue`（types）与 `hasFunction`（functions）；
- 文档一致性核对：版本表述、规范链接、调试默认值与源码头部注释版本统一
  （规范 §6.6「调试信息默认开」改为默认关，与 `showDebug: false` 对齐）。

### DSQL 更新

**DSQL 语言升级 v1.5：三值语义分家 / 别名唯一性 / 摄取容错**（插件版本不变，仅语言版本升级）：
把三种「无」的语义正交分离。

- 三种「无」正交定义：`0` / `false` 为**正常值**（仅裸真值判断为假，运算照常）；
  `null` 为**空容器**（`字段: ""` / `字段: []` 摄取为 null）；
  **empty 值**为**未赋值**（`字段:` 冒号后无内容），除 `**empty**()` 外一切运算按 null 传播；
- 裸真值判断：`0` 由真改假（empty / null / 0 / false / 空串 / 空数组均为假）；**破坏性**
- `**empty**(x)` 语义收窄为「仅当 x 未赋值时为真」，`""` / `[]` / `0` / `false` / null 均假；
- NUMBER 词法收窄：`1.` 与 `.5` 为词法错误；
- SELECT 别名唯一性：所有 `**AS**` 别名互不相同，且不得与行字段名冲突（致命错误，
  不限是否含 TOTAL 项）；已有看板若用 `价格 **AS** 价格` 这类同名别名需改别名；**破坏性**
- frontmatter 重复键：该文件从结果集剔除并计入 `duplicateKey` 警告，查询继续；
- warnings 结构化 `{ type, message }`，调试页按 `[type] message` 渲染；
- 对已有查询的影响：**部分不兼容**——同名别名与依赖 `0` 为真的裸真值判断需按新语义调整。

**DSQL 语言升级 v1.4：TOTAL 全表聚合 + $变量$ 派生体系**：引入两遍执行模型。

- 两遍执行模型：聚合遍扫描 FROM 全量命中行（恒忽略 WHERE）计算 `**TOTAL**`；
  投影遍 WHERE / SORT / LIMIT / SELECT，`$变量$` 查变量表、裸标识符查行字段；
- 语法：`**TOTAL** (字段|数字) **AS** 别名`（别名强制）；`$变量$` 仅 SELECT 内可引用；
  不可反向引用、TOTAL 内禁止引用变量；别名与行字段同名 → 致命报错；
- 语义：数值求和（null / 缺失跳过、非数值跳过计入 warnings、空表 → null）；
  `TOTAL 1` = 总行数；仅含 TOTAL 项 → 单行「汇总」结果；派生列只读
  （列表视图以辅助信息行展示）；
- 调试新增 AGG 行。

- 测试：80 例（五套件：功能 22 / 数学 29 / DSQL语言 21 / store 4 / 摄取层 4）。

---

## [1.7.003] - 2026-09-07（插件开发版，已随 1.8.0 迁移）

### 插件更新

**派生列只读**：不可内联编辑。

- 派生变量列不可内联编辑；列表视图以辅助信息行展示。

**调试**：新增聚合调试行。

- debug 新增 `aggregates`（调试页 AGG 行）。

**测试**：47 + 15 例（dsql-language 6 例校验、math 9 例数值语义），共 62 例通过。

### DSQL 更新

**两遍执行模型**：聚合与投影分离。

- 第一遍聚合遍扫描 FROM 全量命中行（恒忽略 WHERE）计算 `**TOTAL**`；
- 第二遍投影遍 WHERE / SORT / LIMIT / SELECT，`$变量$` 查变量表、裸标识符查行字段。

**语法**：`**TOTAL** (字段|数字) **AS** 别名`。

- 别名强制；AS 右侧接受 `$var$` 或裸名，归一化存裸名；
- `$变量$` 仅 SELECT 内可引用（WHERE / SORT 报错）；
- TOTAL 操作数内禁止引用变量；不可反向引用；
- 变量表与行字段命名空间隔离；AS 别名与行字段同名 → 执行期致命报错。

**语义**：数值求和与计数。

- 数值求和（null / 缺失跳过、非数值跳过计入 warnings、全无数值 / 空表 → null）；
- `TOTAL 1` = 总行数（COUNT 等价）；
- SELECT 仅含 TOTAL 项 → 单行合成结果（文件名「汇总」）。

**词法**：新增变量与 TOTAL 词法单元。

- lexer 新增 `$var$` variable token 与 TOTAL 关键词。

**规范**：`docs/DSQL-EBNF.md` 升 v1.4。

- 补充变量表查询规则 / 别名冲突规则 / TOTAL 计数 / 过滤后聚合变通方案
  （现归档于 [docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)）。

---

## [1.7.002] - 2026-09-07（插件开发版，已随 1.7.0 迁移）

> 移动端调试信息改标签页

### 插件更新

**结果区改为标签页**：调试信息独立滚动。

- 「查询结果 / 调试信息」两个标签切换——原 `<details>` 折叠面板
  在移动端跟随正文长列表无法单独滚动，现调试列表限高 45vh 独立滚动。

**`showDebug` 默认值改为关闭**：已保存过设置的用户不受影响。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3（DSQL v1.4 由后续 1.7.003 引入）。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.7.001] - 2026-09-07（插件开发版，两目录对齐清理）

### 插件更新

**清理**：移除未实装视图类型的死代码。

- 移除 `PLANNED_VIEWS`（board / card / calendar / stats）及其下拉灰显项
  （该死代码仅存在于测试版，正式版本就未包含，清理后两目录对齐）；
- 未实现功能的规划统一记录在工作区根目录 `TODO` 文件（当前留空）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.7.0] - 2026-09-07

### 插件更新

**最低版本要求提升为 Obsidian 1.4.4**：修正官方 API 版本声明。

- 属性编辑依赖的 `fileManager.processFrontMatter` 官方标注 @since 1.4.4，
  原 1.4.0 声明在 1.4.0–1.4.3 上会运行报错。

**稳定性修缮**：异步与索引重建收敛。

- `workspace.revealLeaf` 全部改为 `await`（该 API 自 1.7.2 返回 Promise，
  确保视图完全加载、避免 deferred leaf 未就绪）；
- `metadataCache.on("resolved")` 改为仅首次全量重建，之后走增量路径
  （该事件在启动后每次批量修改解析完成都会触发，原先每次都全量扫描）。

**写法现代化**：以下写法均为官方推荐形式。

- `vault.getAbstractFileByPath` → `getFileByPath`；
- `workspace.getLeaf(true)` → `getLeaf('tab')`。

**结果区改为标签页**：调试信息独立滚动。

- 「查询结果 / 调试信息」两个标签切换——原 `<details>` 折叠面板
  在移动端跟随正文长列表无法单独滚动，现调试列表限高 45vh 独立滚动。

**「显示 DSQL 调试信息」默认改为关闭**：已保存过设置的用户不受影响。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.6.001] - 2026-09-07（插件开发版，已随 1.7.0 迁移）

### 插件更新

**minAppVersion 1.4.0 → 1.4.4**：修正官方 API 版本声明。

- 属性编辑依赖的 `fileManager.processFrontMatter` 官方标注 @since 1.4.4，
  原声明在 1.4.0–1.4.3 上会运行报错。

**`workspace.revealLeaf` 全部改为 `await`**：确保视图完全加载。

- 该 API 自 1.7.2 返回 Promise，官方要求 await 以确保视图完全加载（避免 deferred leaf 未就绪）。

**`metadataCache.on("resolved")` 仅首次全量重建**：避免重复全量扫描。

- 该事件在启动后每次批量修改解析完成都会再触发，原先每次都全量扫描；
  之后统一走增量路径（flush 已重算反向链接）。

**现代化写法统一**：替换为官方推荐形式。

- `vault.getAbstractFileByPath` → `getFileByPath`（带类型返回值）；
- `workspace.getLeaf(true)` → `getLeaf('tab')`（字符串形式为推荐写法）。

**已知不修**：`processFrontMatter` 保存会规范化重写整个 YAML（官方行为，属性弹窗文案提示即可）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [工程调整] - 2026-09-07（未升版本，随 1.6.0）

### 插件更新

**测试按示例三大类重组**：拆分原 `sql.test.ts`。

- `tests/sql.test.ts` 拆分为 `feature.test.ts`（功能示例 22 例）、
  `math.test.ts`（数学示例 9 例）、`dsql-language.test.ts`（DSQL语言示例 12 例），
  共享数据提取到 `helpers.ts`；
- 连同 `store.test.ts` 共 47 例全部通过。

**演示库各自独立**：两库内容一致但只部署本工程插件。

- 两个测试库内容一致（示例数据与预置看板按 功能示例 / 数学示例 / DSQL语言示例 三大类组织），
  但各自只部署本工程插件（曾尝试共用同一 vault，实测报错，已回退）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.6.0] - 2026-09-07

### 插件更新

**frontmatter 属性编辑**：视图内直接改属性并自动刷新。

- 表格视图双击单元格直接改属性（回车保存，自动转型）；
- 列表视图点击条目或行内 ✎ 打开 YAML 属性编辑弹窗（官方 processFrontMatter 写回）；
- 保存后索引增量更新、查询结果自动刷新。

### DSQL 更新

**DSQL 语言规范升级 v1.3**：空优先级与自动列排序的行为明确。

- 空 `**BY** ()` 视为无自定义优先级并计入警告；
- SELECT `*` 自动列按 UTF-8 字节序排序；
- 排序方向语义文档勘误（末尾方向属于最后一个键）。

---

## [1.4.001] - 2026-09-07（插件开发版，已随 1.6.0 迁移）

### 插件更新

**frontmatter 属性编辑**（看板数据直接改，保存后索引增量更新、查询结果自动刷新）：
全链路走官方 API。

- 表格视图：双击可编辑单元格（直接 frontmatter 字段）→ 输入框 → 回车保存
  （Esc 取消、失焦还原）；空值 → null，true / false 字面量、数字自动转型，其余存为字符串；
  数组列不可内联编辑；
- 列表视图：点击条目打开属性编辑弹窗；表格行悬停出现 ✎ 铅笔，点击同样打开；
- 属性弹窗：官方 API 全链路——`metadataCache` 读 → `stringifyYaml` 展示 →
  `parseYaml` 校验（失败显示错误不写入）→ `fileManager.processFrontMatter` 原子写回；
  Ctrl / Cmd + Enter 快捷保存；
- 官方 API 核对：`processFrontMatter`（obsidian.d.ts:2954）、`parseYaml`（:4817）、
  `stringifyYaml`（:6815）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [DSQL 1.3.001] - 2026-09-07（语言版本修订，插件仍为 1.4.0）

### 插件更新

**调试 `from` 描述明确**：口径写清。

- 明确为「源解析后的命中行数（去重后）」。

### DSQL 更新

**评审项落地（H1–H3 / M1–M3 / L1–L4）**：EBNF 勘误与语义补全。

- EBNF 勘误：`OP_ADD` 多余空格；优先级表 `%||%` 管道转义；
  `sort_item` 文法改为 `{ sort_modifier }`（方向与 **BY** 各至多一次、先后不限，与实现一致）；
- 语义补全：`comparison` 裸操作数真值语义正式写入；键级方向继承子句级方向、均无默认 **ASC**；
  数字不支持科学计数法与负数字面量（词法即如此，补文档）；
- 行为微调（含实现改动）：空 `**BY** ()` 视为无自定义优先级并计入 warnings；
  SELECT `*` 自动列排序改用 UTF-8 字节序（原为 JS 默认序，增补平原字符集外更严谨）；
- 对已有查询的影响：**兼容**（仅空 `**BY** ()` 会新增一条 warning）；
  对应的语言规范章节同步更新（排序方向、真值语义、词法边界）；
- 规范 §7 新增示例：键级方向 + 子句级方向 + 多个 custom_order 组合。

- 测试：41 + 4 例通过（新增空 BY 警告、自动列字节序 2 例）。

---

## [1.4.0] - 2026-09-07

### 插件更新

**设置与视图下拉**：新增小数位数设置，收敛视图选项。

- 设置新增「小数显示位数」（非负整数，默认 4，仅显示层）；
- 视图下拉仅保留已实装的表格 / 列表。

**新增效果验收 vault**：构建自动同步插件产物。

- 新增 `test-vault/`（构建自动同步插件产物）。

**演示数据扩充**：看板即点即看。

- `data.json` 预置 12 个看板（任务 2 / 演示 6 / 跨项目 4）；
- 演示库扩充为 Projects/Alpha、Projects/Beta、Archive/2025、Inbox 多项目结构，
  任务笔记补互链（outlinks 反查可出结果）与日期 / 布尔 / 数组字段。

**调试信息合并**：字段缺失提示并入投影期。

- 查询结果调试信息并入 SELECT 投影期字段缺失提示（面板收集后与执行期合并去重）。

- 测试：39 + 4 例（新增子句语义 6 例、跨目录多组数据 6 例）。

### DSQL 更新

**DSQL 子句改为按前件关系解析**：书写顺序自由。

- 修复面板报「预期 `**SELECT**`，实际为 `**FROM**`」：每条子句至多一次，
  `**FROM**` 成为唯一必填子句，`**WHERE**` / `**SORT**` / `**LIMIT**` 以 `**FROM**` 为前件；
- `**SELECT**` 可省略（默认全字段）；
- `**WITHOUT** **ID**` 可写在任意子句位置，子句重复出现时报错（带行列号）；
  前件缺失报「需要 `**FROM**` 作为前件」，同样带行列号。

---

## [1.2.0] - 2026-09-07

### 插件更新

**看板数据统一存插件 data.json**：移除未使用的 DataShow 目录约定。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.1.0] - 2026-09-07

### 插件更新

**列表视图与自动列**：查询支持省略 `**SELECT**`。

- 查询支持省略 SELECT 的列表视图与全字段自动列；
- 测试数据与示例看板完善。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.0.1] - 2026-09-07

### 插件更新

**移除旧版兼容**：迁移函数替换为纯校形。

- 删除 `migrateBoard`（sections.data → sql 的开发期数据迁移），
  替换为纯字段校形 `normalizeBoard`；
- v1.1 时代遗留数据不再做任何回退转换。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.0.0] - 2026-09-07

### 插件更新

**首个正式版本**：看板、索引与视图齐备。

- 看板（名称 / 类型 / 说明 / DSQL）、看板侧栏与面板；
- metadataCache 增量索引；
- 表格 / 列表视图；
- 正式版附 `versions.json`、干净的用户 README 与 DSQL 规范文档；
- 工作区拆分为 `data_show/`（正式版，插件 id `data-show`）与 `data_show_test/`
  （测试版，插件 id `datashow-dev`），二者可在同一 vault 共存；
- 正式版清理开发痕迹：统一文件头、移除测试 / 方案 / test-vault 等开发产物。

### DSQL 更新

**DSQL v1.2 标记语法**：作为首个正式版本的语言基线。

- 关键词 / 函数 `**WORD**` 包裹、运算符 `%op%` 包裹、字符串单引号、路径双引号；
- 对已有查询的影响：作为初始版本，无历史查询需要兼容。

---

## [0.8.0] - 2026-09-07

### 插件更新

**调试扩展**：新增结构化告警与源统计。

- 调试扩展 `warnings`（非致命问题 + 次数）与 `sourceStats`（逐叶子源行数），
  补充 where 剔除示例（≤3）与 sort 比较次数；解析错误统一 `[DSQL]` 前缀；
- 测试 29 + 4 例。

### DSQL 更新

**v1.2 修订 2**：优先级、大小写与边界语义明确。

- 数据源 **AND 优先于 OR**（两级文法，括号可覆盖）；
- `**BY**` 优先级改为作用于其书写的排序键；
- `**contains**` 改为**区分大小写**（数组严格 `===`，字符串子串），
  忽略大小写用 `**contains**(**lower**(字段), '值')`；
- 乘方结果非有限数 → null；
- null 比较语义明确（参与比较 → false，`%!=%` 取反，`null == null` → true）；
- 对已有查询的影响：**部分不兼容**（AND / OR 优先级、`contains` 大小写敏感性的结果可能变化）。

---

## [0.7.0] - 2026-09-07

### 插件更新

**测试重写**：25 + 4 例。

### DSQL 更新

**DSQL v1.2 大版本（标记语法字面化）**：语法全面标记化。**破坏性**

- 关键词 / 函数 `**WORD**` 包裹、运算符 `%op%` 包裹、字符串单引号、路径双引号；
- 未知 `**WORD**` 词法报错；`**SELECT**` 成为必须子句，
  视图 `TABLE` / `LIST` 缺省 `TABLE`，`*` 全字段；
- 投影支持任意表达式 + `**AS**` 别名（无别名时：字段用路径名、表达式为「列N」）；
- 完整表达式（乘方右结合、取模、连接 `%||%`、比较族、一元正负）；
- 非致命语义问题（类型不匹配 / 除零 / 缺字段）→ null；
  调试对象 `{ from, where, sort, limit, fieldMisses, executionTimeMs }`；
- 内置函数新增 sqrt / cbrt / root(x, n)；
- 多级排序（键级方向）；
- 对已有查询的影响：**不兼容**——所有查询须改用标记语法书写。

---

## [0.6.0] - 2026-09-07

> SORT BY 优先级 + 调试信息

### 插件更新

**调试信息与设置开关**：测试 32 + 4 例。

- 执行期调试收集（FROM 命中 / WHERE 过滤 / SORT 详情 / LIMIT 截断 / FIELD 字段缺失）；
- 设置新增「显示 DSQL 调试信息」开关（默认开，面板折叠展示；1.7.0/1.7.002 起改默认关）。

### DSQL 更新

**`**SORT**` 字段 `**BY**` 自定义优先级**。

- `**SORT** 字段 **BY** ('值1','值2',...)` 自定义优先级（此版本作用于首键，
  0.8.0 起改为作用于其书写的排序键）；
- 规范文档升 v1.1。

---

## [0.5.0] - 2026-09-07

> 排序规范化 + 测试补全 + EBNF

### 插件更新

**测试与文档**：测试扩至 DSQL 25 例 + store 4 例；新增 DSQL 规范文档 v1。

- 期间修正解析器「多余子句静默忽略」的缺陷，并补 EOF 校验。

### DSQL 更新

**排序规范化**：`**SORT**` 改 UTF-8 字节序确定性排序。

- 逐字节比较、公共前缀递归下降、短者在前；数字按数值、null 恒排末尾；
- `=` / `!=` 定义为字节精确匹配（区分大小写）。

---

## [0.4.0] - 2026-09-07

> 面板编辑化（DSQL 定名）

### 插件更新

**面板编辑化**：DSQL 编辑器移入看板面板。

- 看板模型精简为 `名称 + 类型 + 说明 + SQL`，模板 / 规则彻底移除；
- DSQL 编辑器移入看板面板（等宽输入、防抖 500ms 自动保存）；
- 工具条：刷新按钮（手动重跑）+ 视图类型下拉（表格 / 列表实装，可覆盖语句类型并持久化；
  看板 / 卡片 / 日历 / 统计标为规划中项，1.7.001 已清理）；结果区独立刷新不打断编辑。

### DSQL 更新

**查询语言正式定名 DSQL**（DataShow Query Language）。

---

## [0.3.0] - 2026-09-07

> 索引层 + DSQL 前身打通

### 插件更新

**索引层打通**：全量首扫 + 增量监听。

- 看板组成收敛为**数据 / 模板 / 规则**（关系由模板表达），
  SQL 从散落各笔记改为插件集中管理；
- 索引层 metadataCache 全量首扫 + `changed/deleted/renamed` 增量监听（300ms debounce）；
- 行仓库（笔记 = 行，frontmatter = 列，`file.*` 虚拟列含出 / 入链），订阅通知。

**面板新增「查询结果」区**：表格 / 列表渲染、文件名点击跳转、索引变化自动重跑。

- 测试：store 4 例。

### DSQL 更新

**自研 SQL v1 子集**：DSQL 前身。

- 语法 `TABLE|LIST ... FROM 路径/#标签 [OR|AND] WHERE 表达式 SORT LIMIT`；
- 内置 contains / length / lower / upper / empty，支持中文（Unicode）标识符，错误带行列号；
- 测试：10 例；DSQL 语法参考写入演示库 README。

---

## [0.2.0] - 2026-09-06

### 插件更新

**看板模型引入**：看板改为在插件设置中定义。

- 看板改为在**插件设置**中定义（名称 / 类型 / 作用描述）；
- 首次安装自带空白「默认看板」；
- 侧栏改为展示设置中的看板（按类型分组），点击在主工作区打开；
  `DataShow` 目录此时仅作数据约定（1.2.0 起移除）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：查询语言尚未定名。

---

## [0.1.0] - 2026-09-06

### 插件更新

**插件骨架**：调研与脚手架。

- 前期调研 Dataview / Datacore / Breadcrumbs 源码与维护现状，产出架构方案
  （五层单向依赖 + ADR 决策记录；方案文档 `datashow方案.md` 后续已废弃删除）；
- P1 脚手架（manifest / esbuild / tsconfig / test-vault 热更新）；
- 插件设置页（常规项）；
- 侧栏树（`DataShow` 目录下 .md 即面板、子目录即分组，事件自动刷新）
  与主区面板视图骨架（board 视图骨架 + 正文 Markdown 渲染）；
- ribbon 图标与命令。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：查询语言尚未定名。
