# DataShow 更新日志

> 版本按迭代顺序倒序排列，每个版本条目固定分为「插件更新」与「DSQL 更新」两类；
> 编写规范见 [CONTRIBUTING.md](CONTRIBUTING.md) §6。

## [2.3.0] - 2026-09-15

### 插件更新

**演示库扩充：新增「域示例」类七个看板与配套素材**（`示例/域示例/`）。

- 素材 7 篇：人物/傻青、人物/王五（`wolf` 数组）、武器/狼牙棒·金针·铁针、剧情/苍蓝传（`chart` 数组）、
  内层/暗器（嵌套子域演示专用）；两库字节一致（CRLF）；
- 七个看板：基础·单子域 / 基础·多子域笛卡尔积 / IN·逐元素判断 / DIFF·差集 / 混合·IN + DIFF /
  嵌套·同名域不遮蔽 / 视图·列表三域展开；侧栏分类数 11 → 12、看板总数 67 → 74；
- 调试信息页新增 `DOMAIN` 行（子域行数 / 行展开基数 / `**YIELD**` 槽位摘要）。

**域扩展输出显示口径定稿（只改显示层）**：三视图、CSV、结果区搜索文本共用 `render/format.formatCell`，调用方零改动。

- **列标签**：`<域>` 是**类型标记** → 保留尖括号（`<人物>`）；`$槽位$` 的 `$` 是**命名空间标记** →
  输出层去掉（`$未引用$` → `未引用`）；裸标识符 `**AS** 任务` 不变。去 `$` **只在输出层生效**，
  语言层引用仍须写 `$变量$`；表头 / CSV 表头 / JSON 键 / 搜索文本共用同一份标签；
- **域对象**：`[字段名:值][字段名:值]…`——每字段独立 `[...]` 包裹、`名:值` 冒号无空格、字段之间紧邻无分隔符；
  字段顺序 = 子域 `**SELECT**` 声明顺序；数组值内部 `,` 无空格（`[name:傻青][wolf:狼牙棒,金针]`）；
- **槽位值**：直接输出值，不带 `$`、不加 `[]`——布尔 → `true` / `false`，数组 → `,` 无空格（`金针,铁针`），
  标量原样，`null` / empty → `—`（域对象字段同口径）；
- **口径隔离**：布尔与数组的域口径**只作用于域扩展输出**，其余查询维持既有口径（`是` / `否`、`, ` 连接）——
  既有看板显示零变化。`formatCell` 是「值 → 文本」唯一出口但看不到列来源，故：域对象靠**值形态**辨认
  （唯一的对象值形态），`$槽位$` 值由执行层包 `DomainSlotValue` 显示标记（`@dsql/types`）辨认，
  JSON 导出经 `toJsonValue` 解包为原值；
- **JSON 导出**：域对象仍输出**结构化对象**（字段 → 值）、槽位输出**原值**（布尔 / 数组），
  `null` / empty → `null` 且**保留键**，不做 `[...]` 文本化；CSV 中域对象文本含逗号，按 RFC 4180 自动加引号。

**内部重构（行为零变化）**：DSQL 语言层拆分出三个低层模块，消除潜在循环依赖。

- 新增 `src/core/dsql/expr.ts`：表达式求值（`evaluateExpr` / `resolveField`）从 executor 迁出；
- 新增 `src/core/dsql/source.ts`：FROM 判定与 `[ext]` 读取范围收集（`matchSource` / `matchFolder` /
  `collectSourceFolders` / `collectExtFilters` / `EXT_ALL`）从 executor 迁出；
- `src/core/dsql/coerce.ts` 扩为「§6.3 值语义单一事实源」：并入 `looseEquals` / `compareOrdering` /
  `truthy` / `compareUtf8` / `stringValue` / `stripEmpty` / `isNonPrimitive`；
- 旧算法的公开出口全部由 `executor.ts` 原样再导出（`@dsql/executor` 的引用方零改动）。

### DSQL 更新

**域扩展（DSQL 2.6）：块 `{ }` 语法 + `<域>` / `::` / `**YIELD**` / `**IN**` / `**DIFF**`。**

- **定位**：在旧算法之上叠加一层多数据集模型——块内声明若干**子域**（各自一条子查询），
  子域之间用 `**YIELD**` 的 `**IN**` / `**DIFF**` 建立逐行关系，最后按行展开投影；
  **无块的查询完全走 v2.5 旧算法，输出逐字节一致**（非破坏性、纯新增）；
- **词法层硬约束**（不下放）：`<` 与 `>` 之间只允许一个 IDENT；`<域>::<域>` 报「`::` 右侧须 IDENT」；
  `<域>::<域>::字段` 由第一个 `::` 拦截（跨域引用只允许一级）；`裸名::字段` 报「`::` 左侧须为域标记」；
  块 `{ }` 未闭合 / 多余 `}` 为词法错误；新增关键词 `YIELD` / `IN` / `DIFF`（关键词表 21 → 24）；
- **作用域与可见性**：`**SELECT**` **宽松**（作用域链内已闭合即可，不限文本位置）、
  `**YIELD**` **严格**（同层子域须写在它之前）——同一「产生完成即见律」在两种扫描位置的表现；
  内层可读外层、外层不可穿透内层（「不向外暴露」）；同名域在不同层各为独立绑定；
- **不可传递性**：顶层 `**SELECT**` 只接受单级 `<域>` / `$槽位$`，`<域>::字段` 在顶层为语法错误
  （顶层声明依赖内层结构会形成循环依赖）；
- **IN / DIFF 语义**：`**IN**` 逐元素判断成员（槽位 (ROW, ROW) → ROW）；`**DIFF**` 左参取全集、
  右参逐行，取「全集 \ 逐行值」并保留左参顺序（槽位 (AGG, ROW) → AGG→ROW）；
  律表 `isRow(a) || isRow(b) ? ROW : AGG_ROW`；行数 = 所有 ROW 位涉及域（`**IN**` 两侧 /
  `**DIFF**` 右侧）行数之积（同域只计一次），无 `**YIELD**` 时 = 所有根块子域笛卡尔积；
- **域对象与投影**：子域逐行投影为「字段名 → 值」对象；`**SELECT** <域>` 取当前行对象
  （未参与行展开时取全量对象）；列名保留 `<域>` / `$槽位$` 形态；`**YIELD**` 项缺省 `**AS**` 即无绑定、
  计算照常执行但不投影；
- **错误处理**：词法 / parse / 语义三层分离，逐条文案见规范 §6.13.8（含「未在该 `**YIELD**` 之前声明」
  /「不向外暴露」/「未声明域」/「同层重复声明」/「循环依赖」/「子查询缺少 `**FROM**`」等）；
- **规范与演示同步**：`docs/DSQL-语言规范.md` 新增 §6.13（含 14 条「实现口径声明」逐项报备与设计草案的差异）
  并同步 §1 关键词表 / §2 词法 / §3 EBNF / §4 前件 / §5 优先级 / §6.13.7 显示口径 / §7 示例；
  `test-vault/README.md` 补「可复制示例（域扩展）」章节（含输出显示口径表）与素材清单。

测试：344 例（十四套件）全部通过；两库 74 个看板 parse 巡检 73 通过（唯一失败为既有的
`fx4-syntax-tighten` 设计报错看板）。

**已发布查询完全不受影响**（无块 → 旧算法；新标记在 v2.5 中均无用途，裸标识符 `in` / `diff` / `yield`
与字符串 `'**IN**'` 不受影响）。

## [2.2.1] - 2026-09-15

### 插件更新

**两池同名列的预览与导出列名口径统一**：表格视图表头改为复用导出层 `exportHeaders`。

- 跨池同名列（裸标识符标签 与 `$变量$` 标签 / TOTAL / COUNT 槽位）在预览表格与 JSON / CSV 导出中统一以 `_N` 后缀消歧；
- 修复「预览显示两个同名列头、导出却自动改名 `x` / `x_2`」的不一致，列名契约单一来源（`@render/export`）。

### DSQL 更新

**两池隔离修复（DSQL 2.5）：`$变量$` 列标签不再与裸标识符列标签跨池判重**。

- 此前 parser 用同一张 `aliasTokens` 映射登记裸标签与 `$变量$` 标签，导致 `SELECT owner **AS** x, 成绩 %+% 1 **AS** $x$` 这类跨池同名被误报「别名重复」；
- 现改为**分池管理**：裸标签走行字段池（`aliasTokens`），`$变量$` 标签走变量池（`varAliasTokens`），跨池之间不互相比对；
- 同池内列标签仍须互不相同、重复即报错；TOTAL / COUNT 槽位 / 普通 `expr AS $x$` 与裸 `AS x` 同名均合法、各成其列（两池隔离，§6.7）；
- 新增回归测试「裸 AS 标签与 `$变量$` 标签同名共存（DSQL 2.5）」；§6.7 唯一性规则改为按池判定并增补跨池同名正例，EBNF 注释同步收紧。

测试：296 例（十三套件）全部通过。

**本版本功能逻辑与 2.2.0 完全兼容**。

## [2.2.0] - 2026-09-14

### 插件更新

**侧栏搜索栏**：看板侧栏顶部新增搜索栏（输入框 + 搜索 + 清空），按「分类名 / 看板名」不区分大小写子串过滤。

- 占位符 `请输入分类或规则名称`；点「搜索」或回车应用，点「清空」恢复全部；
- 折叠状态以全量分组为准：过滤期间不丢、不误删用户的折叠（重渲染时按全量分组清理死折叠记录，再按 `collapsedGroups` 原状态渲染）；
- 无匹配的分类整组隐藏；全都不匹配时显示「没有匹配的看板」；搜索词仅会话内保留（不写 `data.json`）；
- 实现仅涉及 sidebar-view，看板定义与查询结果零改动。

**中间搜索栏扩展为三视图通用**：原仅卡片视图生效的搜索，现对表格 / 列表 / 卡片三视图统一生效。

- 占位符改为 `🔍 搜索...`；匹配口径 = 每行可见文本（文件名标题 + 全部列名 + 全部列显示值），不区分大小写子串；
- 三视图的行统一加 `.datashow-row` 标记 + `dataset.searchText`，过滤器共用一套 DOM 后置隐藏逻辑（不再每视图写一套选择器）；
- 搜索词在切视图时保留（2.1.1 曾规定切到非卡片视图清空，本次改为保留）；store 自动刷新 / 防抖重跑后过滤自动重新套用；
- 表格 / 列表视图下不再灰显禁用，与卡片视图行为一致。

**结果导出**：面板工具条右侧新增导出（vault 相对路径输入框 + 导出按钮），把当前结果导出为 JSON / CSV。

- 路径为 vault 内相对路径（允许前导 `/`，`/` 与 `\` 同作分隔符），自动创建缺失目录；
- 合法扩展名 `.json` / `.csv`：`resolveExportPath` 拒绝绝对路径（`C:\` / `//`）、`..` 越界、非法字符与 `.xlsx`
  （`.xlsx` 提示「暂不支持，请导出 .csv 后用 Excel 打开另存」，不引入第三方表格库）；
- 导出内容 = 搜索过滤后的行集（搜索框为空则为全部行），三视图导出同一份列结构（SELECT 投影，可选「文件」列）；
- JSON 保留原始类型、CSV 按 RFC 4180（逗号分隔、CRLF、按需引号转义）；成功提示 `已导出 N 行到 <路径>`，失败提示原因；
- 纯函数 `src/render/export.ts`（`resolveExportPath` / `toJSON` / `toCSV` / `buildExport`，零宿主依赖）+ 宿主接口 `IExportHost.writeExport`（v2.2.0 新增第八条宿主接口，`host/obsidian/export-host.ts` 经 `normalizePath` + 递归 `createFolder` 写入并越界校验）。

### DSQL 更新

本次无新增语法子句（语言版本仍为 v2.4）；含规范澄清、v2.4 内的一处**收紧**、一处**修复**与演示库扩充。

**规范澄清 / 收紧（文档 + 实现）**：

- `§2` `EXT_FILTER` 注释改写为「[ ] 的两种语法角色」：`[ext]` 后缀过滤原子（§6.9）与 `**WHILE**`
  迭代边界（§6.12）在词法层不做语义区分——遇 `[` 起收集、扫描至**首个** `]` 止（**无 STRING 感知**，
  合法查询的括号内不含 STRING）；括号内换行（LF / CRLF / CR）折为一个空格
  （`readExtFilter` 同步处理 `\r`，CRLF 视作单个换行、不双计行号）；
- `§4` 新增「子句作用域区间」：子句自其关键字起、至下一个子句起始关键字止；
  SEARCH 的 `STRING **AS** ident` 为区间内专属，跨区间即语法错误；
- `§6.10` 新增「作用域（区间专属）」段与两类 parse 期错误对照表（**语法结构错误** vs **名称绑定错误**，
  报错文案引实测：前者为「多余的查询子句『AS』」——STRING 本身是合法表达式原子），
  并明确 SEARCH 的 `**AS**` 别名是裸 ident（行字段）、非 `$变量$` 槽位；
- `§6.2` 补「裸标识符求值顺序」明文：`file.*` →（SEARCH 产出字段 ∪ frontmatter 键，同名已于
  prepare 期致命）→ `null`（并说明为何不用箭头串——两者对等并列，非优先关系）；
- `§6.7` 新增「两池互不越界——三堵墙」（读写墙 / 循环依赖墙 / 结构墙）与两类 parse 期错误分法；
- `§1` 关键词清单由单行代码改为**分组表格**并声明「共 21 个，下表即全部」：子句 11 + 视图 3 + 逻辑/别名 4 +
  排序方向 2 + 聚合项 1（`TOTAL`，仅 SELECT 项内合法）。已与 `lexer.KEYWORDS`（20 个）+
  `AGG_KEYWORDS`（`TOTAL`）逐项对照——**无遗漏、无多余**；同时明示 DSQL **没有** `GROUP BY` / `HAVING`
  （整集合聚合由 `TOTAL` 承担、过滤后计数由 `COUNT` 承担），也不含已废除的 `TABLE` / `LIST`
  （实测 `**GROUP**` / `**HAVING**` → 词法期「未知关键词」）；
- `§3` `query` 产生式注释补「`SELECT` / `WITHOUT ID` / 视图无前件，但整条查询必须含 `FROM`」；
- `§4` 新增「**「无前件」不等于「不需要 FROM」**」段 + 错误示例：前件为「—」的子句单独出现时由
  **收尾检查**（读到 EOF）报「缺少 `**FROM**` 子句（数据源），实际为 文件结束」（列号指向 EOF，非子句起始），
  与前件错误是两条分支两种文案；并补 `SELECT` / `FROM` **子句重复**的实测示例（前件 / 重复 / 收尾三类错误均已实测复核）；
- `§6.7` / `§6.11` 口径措辞统一为「整集合的单标量」，明确 **TOTAL 恒忽略 WHERE**、
  **COUNT 为 WHERE 过滤后行集口径**（无 WHERE 时退化为 FROM 全量），避免误读为 COUNT 也忽略 WHERE；
- **`§3` / `§6.12` `**WHILE**` 边界收紧为 NUMBER 字面量（v2.4 内收紧，破坏性）**：两边界须为
  非负整数且 起始 < 结束，**全部改为 parse 期静态校验**（此前为运行期逐行求值、允许任意表达式）；
  字段 / 函数 / 算术 / 字符串 / `$变量$` 边界不再合法，错误分三类（非 NUMBER / 非非负整数 / 起始 ≥ 结束）；
  单测同步调整（WHILE 套件 21 → 20 例）；
- `§6.12` 新增「**N 与 `search_item` 个数无关**」明文 + 对照示例：`**WHILE** [起始, N]` 的轮数只由边界
  之差决定，多字段是「同一轮内多模板各取一个匹配」，**不因字段数增大 N**；`§6.10` 的两 item 示例补注
  （两组正则仍只配 `[0, 1]` → 每篇 1 行）。实测：该示例 `[0, 1]` 与 `[0, 2]` 在演示库上同为 2 行
  （第 2 轮两模板均无匹配 → 补 null → 被 WHERE 滤掉），差异只在「有没有可能取到第 2 个匹配」。

**修复**：

- **变量表恒非空（`expr **AS** $变量$` 链式派生不再依赖 TOTAL / COUNT）**：此前变量表只在查询含
  `**TOTAL**` 或 `**COUNT**` 时才创建，而三个视图渲染器以 `result.globals` 是否存在决定是否为每行
  克隆变量环境 → **无聚合项的查询里链式派生静默失效**（`$小计$ %*% 0.8 **AS** $折扣价$` 恒为 `—`），
  与规范 §6.7「前面的派生变量可被后面的列引用」不符。现改为查询开始即建空表（`createState`），
  `ResultSet.globals` 恒非 null；`tests/math.test.ts` 的链式派生用例改为直接断言 `r.globals`
  （此前用自建 `new Map()`，掩盖了该缺陷）。
- **跨池误判废除：`**AS** $变量$` 不再与行字段 / SEARCH 别名判冲突（两池隔离）**：此前 parser 用同一张
  别名表校验 SEARCH 别名、`checkSelectAliases` 又把变量别名并入行字段池，于是
  `**SELECT** 姓名 **AS** $x$, 待办 … **SEARCH** '…' **AS** x` 被误报「SEARCH 别名与 SELECT 别名冲突」，
  与 §6.7「两套命名空间完全隔离」自相矛盾。现改为：SEARCH 别名只与**裸标识符列标签**比对
  （两者同行字段池）；执行期列标签校验只覆盖裸标识符标签（新增 `ColumnSel.aliasVar` 标记变量别名，
  TOTAL 别名一并标记）；报错文案区分「列标签」/「变量」。规范 §3 / §6.7 / §6.10 同步：列标签唯一性
  与行字段池唯一性拆成两条规则，并新增「两池重名不构成冲突」明文与正例。
- **数值族函数统一到 §6.3 隐式转换**：`**sqrt**` / `**cbrt**` / `**root**` 此前只认 number，于是
  `**sqrt**('16')`、`**sqrt**(年龄)`（SEARCH 抽取的数字串）恒为 null，而同一份数据的 `'16' %+% 1` = 17
  ——同一语言两种口径。现抽出 `src/core/dsql/coerce.ts` 承载 `toNumber`（算术 / 比较 / 排序 / TOTAL /
  数值函数共用单一事实源），函数与算术口径一致（数字串、布尔可转；数组 / 空串 / 全空白 / 非数值串仍 null）；
  一元正负号仍是全语言唯一的「只认数值」例外（§6.3），规范 §6.5 补明文。
- **empty 值（`字段:` 未赋值）不再以内部形态外露**：`formatCell` 此前只判 `value == null`，而 empty 值是
  `Symbol("DSQL:empty")` 哨兵 → 三视图单元格、CSV 导出字段、结果区搜索文本都会写出 `Symbol(DSQL:empty)`；
  卡片内联编辑点击未赋值字段时输入框同样预填该字符串（JSON 导出原已由 `toJsonValue` 归 `null` 并保留键）。
  现 `formatCell`（唯一显示出口）与 `card-view.beginFieldEdit` 均显式判 `EMPTY`——前者显示 `—`、
  后者预填空串；新增一条用例锁四面（渲染 / WHERE / JSON / CSV），规范 §6.3 补「显示与导出口径」。

**实现清理（无行为变化）**：

- `ast.ts`：`ColumnSel.total` 注释改为「聚合项自声明自投影」（原文「仅填充槽位、自身不投影」与
  `resolveColumns` 实际投影相反）；`SortKey.dir` 注释改为「null = 默认 `**ASC**`」（子句级方向已在 v2.1 删除）；
- `executor.ts`：`stageSearch` 头注释不再写「逐行求值边界 / 否则运行期报错」（边界已改为 parse 期静态校验）；
  `whileRounds` 由行循环内提到循环外（结果与行无关）；SEARCH 正则改为**每模板预构造一次**全局副本、
  逐行仅重置 `lastIndex`（原实现每行每模板 `new RegExp`，1000 行 × 3 模板 = 3000 次构造）；
  删除 `stageCount` 中恒为空操作的 `globals ??= new Map()`（`createState` 已初始化）；
- `parser.ts`：删除 `splitTopLevel`（括号深度 + 引号感知的复杂切分），WHILE 边界只接受 NUMBER 字面量、
  不含逗号，改为 `splitWhileBounds` 按**首个逗号**切分——含第二个逗号统一报「**WHILE** 需两个边界」；
- `lexer.ts`：显式注释 `[ ]` 内把 `\r` 当换行、括号外当普通空白这一「同字符两种语义」是有意为之；
- `panel-view.ts`：导出控件注释去掉 `xlsx`（该格式已在 `resolveExportPath` 明确拒绝）；
  `settings/schema.ts`：`viewOverride` 注释的文档引用 `DSQL-EBNF.md` → `docs/DSQL-语言规范.md`（EBNF 已并入）。

**演示库扩充**：

- 新增 `示例/多变量抽取示例/`（`人物档案.md`：单篇正文含 3 段重复的 `姓名：/年龄：/阵营：` 字段块）
  与「多变量抽取示例」类 1 个看板——`**WHILE** [0, 5]` 驱动三组正则各自独立游标、第 k 轮取各自的
  第 k+1 个匹配 → 前 3 轮得张三 / 李四 / 王五，后 2 轮补 null 被 `**WHERE** 姓名 %!=% null` 过滤；
- 新增「WHILE示例」类 8 个**语法变体**看板（分类不变，仍十一大类），覆盖单组 WHILE+SEARCH 的全部
  合法形态：语法·子句自由穿插（SORT→WHILE→WHERE→SEARCH）、语法·列表视图多轮展开（LIST_VIEW）、
  语法·省略 SELECT 自动列 + WITHOUT ID、语法·[ext] 非 md 多轮 + LIMIT、语法·TOTAL 求和多轮抽取的数字串、
  语法·派生变量链（`$变量$` 消费 SEARCH 字段）、语法·排序·SEARCH 字段自定义优先级、
  语法·计数·偏移区间 [2, 5] + COUNT 多槽位；
- 多条 `**WHILE**` / `**SEARCH**` 子句**不支持**（重复即 parse 期致命「子句重复出现（每条子句至多一次）」），
  故演示以**单组 WHILE+SEARCH 的 8 种变体**呈现，多字段由单条 SEARCH 的多个 `search_item` 覆盖；
- 两库同步；看板 59 → 67（分类仍十一大类）。

- **版本兼容声明**：本版本功能逻辑与 2.1.4 完全兼容（数据可读、设置沿用、DSQL 语法不变）。
- 测试：295 例（十三套件：功能 22 / 数学 32 / DSQL语言 30 / store 4 / 摄取层 5 / 索引器 4 / ext后缀过滤 64 / SEARCH 40 / WHILE 20 / COUNT 23 / viewSync 15 / normalizeBoard 10 / 导出 26）全部通过（数学示例套件的链式派生用例改为直接断言 `ResultSet.globals`，锁定「变量表恒非空」；新增数值族函数隐式转换、「SEARCH 别名 vs `AS $变量$` 不冲突」、「empty 值四面口径（渲染 / WHERE / JSON / CSV）」三例）；
  `npm run build` 通过；两库各 67 个看板全量 parse 巡检 66/67（唯一失败为「修复·语法收紧演示」，按设计报错），
  8 个新增语法变体看板以演示库真实文件执行核验输出行数与文案一致；
  已在 test-vault-local 实机验收（Obsidian CLI + DOM 断言：侧栏搜索分类 / 名称过滤与折叠保留、三视图通用搜索、JSON / CSV 导出文件落盘）。

## [DSQL 2.4] - 2026-09-14（语言版本升级，随插件 2.2.0 发布）

### 插件更新

**WHILE 循环驱动子句**：`**WHILE** [起始, 结束]` 为 `**SEARCH**` 提供迭代机制——SEARCH 不再是
「每行只取首个匹配」的一次性抽取，而是由 WHILE 逐轮推进游标、每轮产出一行。

- 前件关系：`**WHILE**` 前件 = `**FROM**`；`**SEARCH**` 前件改为 `**WHILE**`（不再直接依赖 FROM）；
  两者**双向绑定**——只写其一 → parse 期致命；
- 迭代：次数 = 结束 - 起始（两边界为非负整数字面量、起始 < 结束，**parse 期静态校验**）；
  每行固定产出「结束 - 起始」行，匹配不足补 null、不提前停、全 null 行照产（过滤交 WHERE）；
  无轮次变量——起始的绝对值不影响结果（`[0, 3]` ≡ `[2, 5]`）；
- 执行位置：`FROM → [ext] 并入 → WHILE + SEARCH 结构匹配 → 聚合遍（TOTAL）→ WHERE → COUNT → SORT → LIMIT → SELECT`；
- TOTAL 口径延伸：TOTAL 恒基于**匹配后行集**（含补 null 行），恒忽略 WHERE；
  `**TOTAL** 1` 与 `**COUNT** 槽位 %!=% null` 之差 = 补 null 行数（两口径正交）；
- TOTAL 对数字串的隐式转换：SEARCH 产出为原始字符串，`**TOTAL** 金额` 现按 §6.3 隐式转换求和
  （数字串可转、非数值串跳过并计 warnings）——此前只累加真正的数值类型，与规范「数值型抽取字段
  可被 TOTAL 聚合」的表述不一致，本次一并对齐（修复）；
- SEARCH 调试统计改为按**产出单元格**计数（单元格 = 源行 × 迭代轮次）；
- 演示库：新增 `示例/待办示例/`（3 篇会议纪要，含待办勾选行）与「WHILE示例」类 2 个看板；
  既有 7 个 SEARCH 看板补 `**WHILE** [0, 1]`（等价旧行为）；看板 56 → 58、九大类 → 十大类。

### DSQL 更新

**DSQL 语言升级 v2.4：`**WHILE**` 循环驱动子句**（规范 §6.12，文档版本 2.3.1 → 2.4.0）：

- 语法：`**WHILE** [起始, 结束]`——形态唯一（两个 NUMBER 字面量、方括号包裹、逗号分隔；无嵌套 /
  无 `**BY**` / 无方向词）；两边界须为非负整数且 起始 < 结束，**parse 期校验**；片段内词法错误按
  `[` 的位置重定位列号；
- 前件：`**WHILE**` → `**FROM**`；`**SEARCH**` → `**WHILE**`；依赖序 FROM → WHILE → SEARCH 可被其它子句穿插；
- **破坏性**：SEARCH 必须搭配 WHILE——既有 SEARCH-only 查询 → parse 期致命
  （迁移：SEARCH 前补 `**WHILE** [0, 1]`，等价旧行为）；新增 `**WHILE**` 关键词原不在关键词表，
  故无历史查询受影响；裸 `while` / `$while$` / 字符串 `'**WHILE**'` 词法隔离，行为不变；
- 错误：WHILE 无 SEARCH → 「**WHILE** 需 **SEARCH** 配合」；SEARCH 无 WHILE（或写在 WHILE 之前）→
  「**SEARCH** 需要 **WHILE** 作为前件」；WHILE 前件 FROM 未出现 / WHILE 或 SEARCH 子句重复 /
  缺逗号 / 非两边界 → parse 期致命（带行列号）；**边界非 NUMBER / 非非负整数 / 起始 ≥ 结束 →
  parse 期致命**（带 `[` 的绝对列号）。

- 测试：292 例（十三套件：功能 22 / 数学 31 / DSQL语言 30 / store 4 / 摄取层 5 / 索引器 4 /
  ext后缀过滤 64 / SEARCH 39 / WHILE 20 / COUNT 23 / viewSync 15 / normalizeBoard 10 / 导出 25）全部通过；
  `npm run build` 通过；两库各 58 个看板全量 parse 巡检（仅「修复·语法收紧演示」按设计报错）。

## [DSQL 2.3] - 2026-09-14（语言版本升级，插件仍为 2.1.4）

### 插件更新

**COUNT 分类计数子句**：`**COUNT** 比较语句 **AS** $槽位$` 在 WHERE 过滤后行集上逐行求显式比较，
为真的行数（标量）填充 SELECT 声明的 `$槽位$`。与 `**TOTAL**`（恒 FROM 全量口径）形成两口径对照，
占比 / 差值类计算的全部分母与分类分子可同板组合。

- 执行位置：WHERE 之后、SORT 之前；无 WHERE 时口径 = FROM 全量命中行；空行集 → 0 不 warning；
- 槽位模型：SELECT 里裸 `$x$` 项 = 槽位声明（同名至多一次）；TOTAL 项自声明自填充自投影
  （同名的裸槽位不再重复投影）；COUNT 填充目标须已在 SELECT 声明（否则 parse 期致命）；
  未填充槽位静默忽略；声明了未使用的槽位不影响查询；
- `$x$` 引用须指向槽位 / 聚合填充名或更早的输出别名（未声明 → parse 期致命）；
  `$x$` 出现在 WHERE / SORT → parse 期致命（执行在聚合之前，值尚不存在）；
- COUNT 比较语句复用 §6.3 比较口径（null 同一性 / 非原始值守卫 / 隐式数值转换）；
  可引用 SEARCH 抽取字段与 [ext] 并入行（均先于 COUNT 执行）；比较内引用聚合变量 → 循环依赖致命；
- 调试信息新增 `count` 字段（各计数项结果与口径，调试页 COUNT 行）。

**执行顺序调整（覆盖 DSQL 2.2 的表述）**：SEARCH 正文抽取移到聚合遍**之前**——
`FROM 源解析 → [ext] 行并入 → SEARCH → 聚合遍（TOTAL）→ WHERE → COUNT → SORT → LIMIT → SELECT`。
SEARCH 抽取字段对聚合遍可见：数值型抽取字段可被 `**TOTAL**` 聚合（DSQL 2.3 起）。

### DSQL 更新

**DSQL 语言升级 v2.3：`**COUNT**` 分类计数子句与槽位模型**（前件 FROM，执行在 WHERE 之后 / SORT 之前）：

- 语法：`**COUNT** 比较语句 **AS** $槽位$, ...`——独立子句（至多一次、与 WHERE 书写先后自由、
  不进表达式）；`AS` 强制 `$槽位$` 写法且须已在 SELECT 声明；比较必须显式带比较符
  （裸操作数 / 与 AND、OR 组合 → parse 期致命）；比较内引用聚合变量 → 循环依赖致命；
- `**TOTAL**` 的 `**AS**` 同步收紧为强制 `$槽位$`：聚合项自声明自投影，裸名别名 → parse 期致命。**破坏性**
  （既有看板中 `**TOTAL** 成绩 **AS** 总成绩` 的裸名写法需改为 $槽位$ 写法）；
- `$变量$` 引用校验收紧：未声明（未指向槽位 / 聚合填充名 / 更早输出别名）→ parse 期致命，
  错误文案由「未定义」改为「未声明」。**破坏性**（纯文案与校验口径变化，合法查询不受影响）。

- 测试：243 例（十一套件：功能 22 / 数学 31 / DSQL语言 26 / store 4 / 摄取层 5 / 索引器 4 / ext后缀过滤 64 /
  SEARCH 39 / COUNT 23 / viewSync 15 / normalizeBoard 10）全部通过；`npm run build` 通过；
  已在 test-vault-local 实机验收（search示例 类 sr8 / sr9 COUNT 演示板，Obsidian CLI + DOM 断言）。

### 修复（全量代码审查定版，2026-09-14；规范文档版本 2.3.0 → 2.3.1）

**DSQL 语言层**：

- `**SORT**` 方向修饰符重复（如 `**SORT** s **DESC** **DESC**`）→ parse 期致命
  （§3「各 modifier 至多一次」此前未落实）；子句级方向的不可达残留 `SortClause.dir` 同步删除；
- `[ext]` 并置简写补齐：`**WHERE** [txt] **NOT** x %==% 1` 现与显式 `[txt] **AND** **NOT** …` 同构
  （此前报「多余的查询子句」）；
- 词法层落实 `VARIABLE = "$" ident "$"`：`$a b$` 等含空格 / 非法字符的变量名不再被接受；
- `**LIMIT**` 须为整数：`**LIMIT** 1.5` → parse 期致命（§3 定稿）；
- 算术结果非有限数统一守卫：`%/%` `%*%` 溢出（如 `1e308 %/% 1e-308`）→ null + warning，
  与 `%^%` 口径一致；一元正负号定稿为「仅作用于数值，非数值 → null 不计 warning」（§6.3）；
- 别名两分法定稿（§3 / §6.7）：裸标识符 `**AS**` = 列标签（不进入变量命名空间，不可被 `$..$` 引用）；
  `**AS** $变量$` = 派生变量（可被更晚的列引用）。

**摄取 / 索引层**：

- frontmatter 重复键检测不再把 YAML 顶层列表项（`- 键: 值`）误判为顶层键
  （此前合法文件被整体剔除且 warning 字段名错误）；
- 文件删除 / 重命名时同步清除其摄取警告（此前 `duplicateKey` 警告在文件删除后永久残留）；
- 启动全量重建不再双跑：`resolved` 事件仅在行仓库为空时补扫；
  全量重建改为先检重复键再入仓库，重复键文件不再有「先入后删」的可见窗口期。

**执行器 / UI**：

- 查询管线拆分为独立阶段函数（FROM / SEARCH / 聚合 / WHERE / COUNT / SORT / LIMIT），无行为变化；
- WHERE 调试剔除示例改为边过滤边收集（消除 O(n²) 回扫）；字段名并集单次构建复用；
  `compareUtf8` 增加同一性快路径；SEARCH body 预读改并行读取；
- 卡片视图每张卡片独立变量环境（派生列链式求值不再跨行共享，与表格视图一致）；
- 「调试信息」页签在数据自动刷新后保持当前页（`keepTab` 此前被无条件重置）；
- 清理无调用方的 frontmatter 编辑弹窗链路（`IFrontmatterEditor` / `frontmatter-modal.ts`，
  v2.0 起编辑权归卡片视图独占）。

- 测试：243 例（十一套件：功能 22 / 数学 31 / DSQL语言 26 / store 4 / 摄取层 5 / 索引器 4 /
  ext后缀过滤 64 / SEARCH 39 / COUNT 23 / viewSync 15 / normalizeBoard 10）全部通过；
  `npm run build` 通过；规范文档版本 2.3.0 → 2.3.1。

---

## [示例文件夹更新] - 2026-09-14

### 示例文件夹

**新增「审查修复示例」数据与看板**：为全量代码审查定版（DSQL 2.3.1）的行为修复提供可复现的对照样例。

- 新增 `示例/审查修复示例/`（`test-vault/` 与本地验收库同步）：
  - `溢出与一元.md`——`超大: 1e308` / `微小: 1e-308` / `数值: 5` / `数字串: "5"`，
    对照 `%/%` `%*%` 溢出的非有限数守卫与一元正负号的定稿口径；
  - `重复键对照.md`——frontmatter 含真重复键「数值」，对照 `duplicateKey` 剔除与警告字段名的正确性；
  - `顶层列表.md`——frontmatter 为顶层 YAML 序列（`- 键: 值`），对照「列表项不再被误判为顶层键」的修复；
- 新增「审查修复示例」看板 4 个（侧栏新分类）：
  - **修复·溢出守卫与一元正负号**：溢出除法 / 乘法 → —（null，开调试可见非有限数警告），
    `%-%数字串` → —、`%-%数值` → -5，`数字串 %+% 0` → 5（二元算术仍按 §6.3 隐式转换）；
  - **修复·重复键剔除与顶层列表对照**：同目录三篇并排，重复键篇不出现在结果中、顶层列表篇正常进入结果；
  - **修复·[ext] 并置 NOT 简写**：`**WHERE** [txt] **NOT** status %==% '正常'` 与显式
    `[txt] **AND** **NOT** …` 同构（修复前报「多余的查询子句」）；
  - **修复·语法收紧演示（预期报错）**：`**SORT** status **DESC** **DESC**` 故意触碰方向修饰符重复，
    面板按 parse 期致命错误渲染（演示语法收紧后的报错行为，非可用看板）。
- 演示库看板 52 → 56，侧栏分类 八大类 → 九大类；两库看板定义保持一致。
- `test-vault/README.md` 同步：补示例数据行、「审查修复示例」分类说明与 search示例 看板数勘误，
  `npm test` 套件说明更新为十一套件。
- 看板语法对齐 DSQL 2.3 槽位模型（6 个）：`**TOTAL**` 项自声明自投影后，SELECT 内再裸写同名
  `$x$` 槽位即 parse 期致命「重复声明」。`汇总行（TOTAL 计数与求和）`、`占比（TOTAL 忽略 WHERE）`、
  `出场人次汇总（TOTAL）`、`TOTAL·含 [ext] 全量行`、`回归·TOTAL 口径不变`、
  `求值·TOTAL 全量分母与布尔算术` 删除冗余裸槽位行，保留 TOTAL 项与后续派生列
  （`$x$` 引用直接指向填充值，语义不变）；修正后两库 56 个看板 parse 全通
  （仅「修复·语法收紧演示（预期报错）」按设计报错）；
- 「汇总行」看板说明按现实口径改写：执行器仅在 SELECT 全部为 TOTAL / 裸槽位项时才合成单行
  「汇总」（§6.7），该看板含派生列 `$均价$`，故按行输出、汇总值逐行并列展示（`$均价$` 为全表口径）；
  `test-vault/README.md`「可复制示例」中同一表述的标题与说明同步。

- **版本兼容声明**：本项仅新增演示数据与看板，不涉及插件功能与 DSQL 语法变更。

---

## [DSQL 2.2] - 2026-09-14（语言版本升级，插件仍为 2.1.4）

### 插件更新

**SEARCH 正文抽取**：`**SEARCH**` 子句从每行 body 用正则抽取内容挂成行字段，与 frontmatter
字段、`file.*` 平级，WHERE / SORT / LIMIT / SELECT 全部可用。SEARCH 是纯消费者——
读取范围完全由 FROM 决定（纯标签源零 body 读取；禁止全库 body 扫描）。

- 新增 `core/index/body.ts`：正文抽取纯函数——md 按 `frontmatterPosition.end.offset` 剥离
  （去一个前导 `\n`；无 frontmatter → 全文）；非 md 剥掉全部 `---` 围栏行及其之间内容
  （成对翻转、未闭合剥到 EOF、``` 围栏原样保留）；
- `IExtSourceHost` 新增 `readBody`（cachedRead 原文 + md frontmatter 结束偏移）；
  `core/index/ext-source` 新增 `loadBodies`（按 FROM 命中范围预读，随行临时携带、不缓存不常驻）；
- 执行器新增 SEARCH 阶段（FROM → [ext] 并入 → 聚合遍 → **SEARCH** → WHERE），抽取字段
  不写回行仓库（按行克隆 fields）；prepare 期冲突检查（frontmatter 字段并集 / `file.*`）；
- 调试信息新增 `search` 字段：各模板命中数 / 未命中数 / 抽取示例 ≤3（调试页 SEARCH 行）；
- 无 SEARCH 的查询零 body 读取，行为与现状完全一致（索引器零改动）。

**§6.3 算术 / 比较 / 排序补丁**（随 SEARCH 引入的数值字符串语义，对全语言生效）：

- 算术：null / empty 操作数 → null 不计 warning；非原始值（数组）→ null 计入 warnings
  （禁止 `Number([5]) === 5` 式静默转换）；数字串隐式转数字（`"123" %+% 0 = 123`，
  空串 / 全空白 / 非数值串视为转不出 → null + warning）；
- 比较：同载数值比（`"007" %==% "7"` 为 true）、同不转字符串比（UTF-8 字节序）、
  混合 → false；非原始值守卫在一切隐式转换之前（`[5] %==% "5"` 为 false）；
- 排序：复用比较口径（数字串按数值序）；null / empty / 非原始值恒排末尾同侧稳定保序；
  空串 `""` 按 UTF-8 序参与排序（排最前）。

**TOTAL 口径修订（覆盖 DSQL 2.1）**：`[ext]` 行并入移到聚合遍之前。

- `**TOTAL**` 恒基于 FROM 全量命中行——**含 `[ext]` 触发读取的非 md 行**（2.1 中仅统计
  md 行仓库），「全量分母」口径下占比类计算可用；
- 执行顺序随之定为：FROM 源解析 → [ext] 行并入 → 聚合遍 → SEARCH → WHERE → SORT →
  LIMIT → SELECT；恒忽略 WHERE 的口径不变；SEARCH 抽取仍在聚合遍之后，TOTAL 不可见抽取字段。

### DSQL 更新

**DSQL 语言升级 v2.2：`**SEARCH**` 正文抽取子句**（前件 FROM，执行在 WHERE 之前）：

- 语法：`**SEARCH** '正则' **AS** 裸标识符, ...`——子句而非函数（不进表达式、至多一次、
  无括号形式）；出现在 WHERE / SELECT 表达式内 → 解析错误；需要 FROM 前件；
- 求值：有捕获组取 `m[1]`（可选捕获组未匹配回落 `m[0]`），无捕获组取 `m[0]`；
  exec 只取首个匹配；输出原始字符串不做类型推断；无匹配 / 无 body → **null**（不是 empty 值）；
- 词法修订（STRING 全语言生效）：`\\` → **两个**反斜杠原样入内容（原为单个），
  `\'` 仍转义为 `'`，其余 `\x` 原样保留；推论：内容结尾 `\` 个数必为偶数，奇数则字符串未闭合；
- 正则：parse 期编译一次（非法 → 致命错误带行列号）；无 flags、大小写敏感、`.` 不跨行；
  parse 期检测未转义 `\p{` / `\P{`（连续反斜杠奇数个）→ 致命错误（非 u 模式下静默退化为字面 p），
  CJK 请用 `[一-鿿]`；
- body：md 剥 frontmatter + 去一个前导 `\n`（无 frontmatter → 全文）；非 md 剥 `---` 围栏块；
  末尾 `\n` 不规范化（`$` 严格匹配输入末尾，须容忍尾换行时自写 `\n?$`）；
  `file.body` 不进字段表；匹配范围仅 body；
- 冲突分两阶段：SEARCH 别名互同名 / 与 SELECT 别名同名 → parse 期；与 frontmatter 字段并集 /
  `file.*` 冲突 → prepare 期（错误带 SEARCH 别名行列号）；
- §6.3 比较语义修订为「同能转数字 → 数值比」：`"007" %==% "7"` 由 false 变 true，
  `"" %==% 0` 由 true 变 false，`" " %==% ""` 口径同步变化——依赖旧字节精确匹配口径的
  混合类型比较需复检；纯数字比 / 纯字符串（非数值串）比行为不变。**破坏性**

- 测试：204 例（九套件：功能 22 / 数学 29 / DSQL语言 22 / store 4 / 摄取层 4 / ext后缀过滤 60 /
  SEARCH 38 / viewSync 15 / normalizeBoard 10）全部通过；`npm run build` 通过。

---

## [DSQL 2.1] - 2026-09-14（语言版本升级，插件仍为 2.1.4）

### 插件更新

**[ext] 非 md 数据源**：`[ext]` 触发的文件级读取，把 FROM 目录下的非 md 文件接入查询
（md → 官方 metadataCache 路径不变；非 md → 自研解析；两条路径正交，失败文件剔除并渲染）。

- 新增 `core/index/yaml-fallback.ts`：自研 YAML 子集解析器（零依赖、纯函数、Node 可单测），
  围栏共享语义 + 挂起机制 + 计数器收集同名键；围栏数先校验（0 → error、奇数 → warn 整体作废、
  空结果 → error）；只服务非 md 路径，与 md 官方解析互不干预；
- 新增 `core/index/ext-source.ts`：文件级读取编排——读取范围 = FROM 目录集合（纯标签源零触发、
  禁止全库扫描），按后缀分派 md / 非 md 两条正交路径，失败文件剔除并按 UTF-8 字节序排序计数；
- 新增第八条宿主接口 `IExtSourceHost`（`host/types.ts` + `host/obsidian/ext-source-host.ts`，
  查询级使用、无常驻状态）；`IVaultHost.subscribe` 新增 vault 级 modify / create 事件（含非 md）；
- 面板：查询含 `[ext]` 时异步加载并带竞态守卫；解析失效文件以 YAML 值渲染在结果区尾部
  「解析失效 N」（warn / error 同桶，截断到设置上限，label 计数与列表一致）；
- 刷新兜底：vault 变更事件在 views 层接收，当前查询含 `[ext]` 时去抖 300ms 重跑（无 `[ext]` 不订阅重跑；
  非 md 不缓存、不常驻、不监听、不进索引器，索引器零改动）；
- 设置新增 `failedFileListLimit`（默认 3，只接受正整数，非法恢复默认），设置页同步；
- `render/` 层保持零 `import "obsidian"`；`file.outlinks` / `file.inlinks` 对非 md 恒为空数组。

### DSQL 更新

**DSQL 语言升级 v2.1：`[ext]` 后缀过滤原子**（`**WHERE**` 表达式新原子，可 AND / OR / NOT 组合）：

- 语法：`[txt]` / `[txt, mp4]` / `[]`；支持**并置简写**——`**WHERE** [txt] status %==% 'x'`
  等价于 `[txt] **AND** status %==% 'x'`（语法定稿示例写法）；出现在 SELECT / SORT / FROM →
  解析错误（带行列号）；`[` 未闭合 → 词法错误；
- 后缀不做归一化：原样字符串与 `file.ext` 严格相等（`[.TXT]` / `[Txt]` / `[-]` 均按字面）；
  无白名单，解析不了的文件按失败处理；
- 三态收集（`collectExtFilters`）：没写 `[ext]` → null（零触发，行为与 v2.0 完全一致）；
  出现 `[]` → ALL（读 FROM 目录下全部文件，与其它 `[ext]` 同现仍归 ALL）；其余 → 后缀并集；
- 执行语义：WHERE 阶段两步——文件级（FROM 目录 ∩ 后缀 → 按后缀分派建行并入）先于行级；
  行级求值用各节点自己的 exts（`[txt] AND [mp4]` 空结果、`[txt] OR status %==% 'x'` 的
  OR 意图完整保留、`NOT [txt]` 只剩 md 行）；`**TOTAL**` 恒不统计非 md 行；
- 方言差异备注：md 同名键剔除 + duplicateKey warning，非 md 同名键收集为数组（YAML 1.2.2
  序列可含重复元素）；嵌套结构非 md 不可见（`键:` 无子行 → null）。

- 测试：166 例（八套件：功能 22 / 数学 29 / DSQL语言 22 / store 4 / 摄取层 4 / ext后缀过滤 60 /
  viewSync 15 / normalizeBoard 10）全部通过；`npm run build` 通过；
  已在 test-vault-local 实机验收（九个 ext示例 看板按测试功能分类，Obsidian CLI + DOM 断言）。

---

## [2.1.4] - 2026-09-10

### 插件更新

**架构调整为 host 依赖模式**：宿主能力收敛到 `host/` 一层，核心逻辑零 Obsidian 依赖，可按宿主整体移植。

- 新增 `host/types.ts` 作为宿主接口与依赖契约的唯一出口：`IVaultHost`（数据源，全 Promise 语义）、
  `IOpener`（打开笔记）、`IFrontmatterHost` / `IFrontmatterEditor`（属性读写与编辑弹窗，数据侧与 UI 侧分离）、
  `IYamlCodec`、`IStorageHost`（带 key 槽位）、`IUiHost`（提示与日志），另含 `PanelDeps` / `SidebarDeps` / `SettingsTabDeps`；
- 新增 `host/obsidian/` 七个适配器：`vault-host` / `opener` / `frontmatter-host` / `frontmatter-modal` /
  `yaml-codec` / `storage-host` / `ui-host`，是 `import "obsidian"` 的合法位置之一；
- 目录归位：`dsql/*` → `core/dsql/*`（对外别名 `@dsql/*` 不变），`index/{store,row-builder,frontmatter}` → `core/index/*`，
  `index/scanner` → `controller/indexer`（改吃 `IVaultHost`，事件防抖与 `DataStore` 写入都在本层），
  `ui/views/三视图` → `render/*`，`ui/utils/viewSync` → `utils/*`，
  `settings.ts` → `settings/{schema,defaults,normalize}` + `views/settings-tab`；
- 面板与侧栏拆为「视图壳 + 纯 UI」：`views/{panel,sidebar}` 只管 Obsidian `ItemView` 生命周期，
  `render/{panel-view,sidebar-view}` 承担全部 DOM 与交互，两者只经 Deps 通信；
- UI 不再反向依赖插件类：`ui/panel` / `ui/sidebar` / 设置页原先 `import type DatashowPlugin` 的三处反向依赖全部消失；
- 数据链路单向化：`IVaultHost.subscribe → VaultIndexer → DataStore → UI 订阅`，宿主事件不再直达 UI；
- 读写约定统一：读路径返回 `T | null` 由调用方降级，写路径 `reject(Error)` 且 message 可直接展示。
- **行为零变化**：DOM 结构、类名、样式、交互、DSQL 语法与查询语义均未改动；
- `data.json` 升级为槽位结构（`{ "settings": { ... } }`），旧扁平格式仍可读为 `settings` 槽，首次保存自动升级，无需手动迁移；
- 新增 `schemaVersion` 字段（缺省按 `CONTENT_SCHEMA_VERSION = 1`），供后续内容迁移使用；
- 验收：`tsc --noEmit` 零错误，`npm run build` 通过，106 例（7 套件）全部通过。

### DSQL 更新

**语言层下沉为可移植核心**：`src/dsql/` → `src/core/dsql/`。

- 对外别名 `@dsql/*` 保持不变，调用方无需改动；
- 模块内部互引由 `@dsql/types` 改为相对路径，配合新增的 `src/core/dsql/tsconfig.json`（独立 `baseUrl`、空 `paths`），
  使 DSQL 可独立编译与发版，`.d.ts` 中不残留别名；
- `ViewType` / `isViewType` / `IMPLEMENTED_VIEWS` / `VIEW_LABELS` 归属语言层（`utils/viewSync` 只从 `@dsql/types` 取视图类型，不依赖 settings）；
- 语法与行为零变化。

> **版本兼容声明**：本版本与 2.1.3 **完全兼容**（数据可读、设置沿用、DSQL 语法不变）。
> **测试情况**：106 例（7 套件）全部通过。

---

## [2.1.3] - 2026-09-09

### 插件更新

**看板打开逻辑修复**：修复侧栏反复点击看板导致标签页 / 面板无限堆积、直至 Obsidian 卡死的问题。

- 修复前：`openBoard` 在「新标签页打开」开启（默认）时每次点击都 `getLeaf("tab")`
  强制新建标签页，即使该看板已在某标签页打开也不复用，旧标签页不销毁，
  反复点击导致标签栏 / 面板无限堆积；
- 修复后：打开看板前先遍历工作区已有面板标签页（`getLeavesOfType` + `getState` 比对 `boardId`），
  同一看板已打开则直接聚焦该标签页并同步状态，全局同一看板仅占一个标签页；
- 复用模式（「新标签页打开」关闭）行为同步理顺：优先复用已有面板标签页（就地切换看板），
  没有面板标签页时才 `getLeaf(false)` 复用当前活动标签页，仅在必要时切分新面板，
  不再无条件切分；
- 新标签页模式保留：不同看板仍各自开新标签页，但同一看板不会重复开；
- 仅改 `main.ts` 的 `openBoard`，无设置 / 数据结构变更，旧 `data.json` 无需迁移；
- 验收：`tsc --noEmit` 零错误，106 例单元测试全部通过。

**结果区标签页保留**：索引自动刷新不再强制切回「查询结果」。

- `renderResult` 新增 `keepTab` 参数（默认 `false`）：
  `true` 保留当前标签页状态，`false` 强制切到「查询结果」；
- `onOpen` 中的 `store.subscribe` 改为 `renderResult(true)`，
  metadataCache 触发的自动刷新不再打断用户停留的「调试信息」标签；
- 工具条「刷新」按钮与 `render()` 中创建结果区时仍使用 `renderResult(false)`，
  这两类主动操作按预期重置回结果页。

**折叠分组死数据清理**：侧栏重渲染时丢弃不存在的折叠名。

- 分组集合变化（看板类型被重命名 / 删除 / 合并）后，旧的 `collapsedGroups`
  条目不再指向任何分组，按存在性过滤后写回 `data.json`；
- 避免「折叠状态永远找不到对应分组」导致的脏数据；现有用户无需手动迁移。

**卡片字段保存错误可视化**：保存失败时给出可读错误并自动恢复。

- `panel.saveFrontmatterField`：文件不存在或非 Markdown 时改为 `throw`，
  不再静默返回（之前调用方 `onSaveField` 完全无感，掩盖了索引漂移问题）；
- `card-view.beginFieldEdit` 捕获错误 → 渲染为 `⚠️ ...` 红色提示
  （`.datashow-card__field-error`，3 秒后自动恢复原值）；
- 成功路径保持原行为（清空编辑态、显示原值）。

### DSQL 更新

本次无 DSQL 语法 / 语义变更。

- **版本兼容声明**：本版本功能逻辑与 2.1.2 完全兼容。

## [2.1.2] - 2026-09-08

### 插件更新

**侧栏分类折叠**：看板侧栏的分类分组头可点击折叠 / 展开，管理看板较多时清单更清爽。

- 点击分组头切换折叠状态，折叠时隐藏该分类下的看板列表，caret 由 ▾ 旋转为 ▸（带过渡动画）；
- 分组头改为 pointer 光标并加 hover 变色，附 `aria-expanded` 无障碍标注；
- 折叠状态持久化到 `data.json`（`collapsedGroups` 字段，按分类名记录），
  重启 Obsidian 后保留；看板增删触发重渲染时折叠状态不丢失；
- 实现仅涉及 sidebar / types / styles，旧 `data.json` 无需迁移（缺失字段自动补默认值）；
- 验收：`tsc --noEmit` 零错误，106 例单元测试全部通过。

**工具条自适应布局**：面板工具条按面板宽度自适应排布，修复手机上工具条撑破面板导致整页横向滚动的问题。

- 搜索控件（输入框 + 搜索 + 清空）独立为 `datashow-toolbar__search-group` 分组；
- 桌面宽屏：搜索组靠左，弹性 spacer 把「刷新 + 视图选择器」推到右侧成一组；
- 窄屏（面板宽度 ≤520px，手机 / 桌面窄分栏，CSS 容器查询 `@container` 触发）：
  换为两行布局——第一行右对齐「刷新 + 视图」紧贴 DSQL 输入框下方，
  第二行搜索组占满整行（输入框自动拉伸），「查询结果 / 调试信息」与结果区随文档流自然下移；
- 面板标题行窄屏下允许换行，长标题不再横向溢出；
- 采用容器查询而非媒体查询：按面板自身宽度触发，桌面窄分栏与手机全屏一套规则通吃；
- 实现仅涉及 panel / styles，测试全部通过。

### DSQL 更新

本次无 DSQL 语法 / 语义变更。

- **版本兼容声明**：本版本功能逻辑与 2.1.1 完全兼容。

## [2.1.1] - 2026-09-08

### 插件更新

**卡片视图搜索控件**：面板工具条新增搜索功能，仅对卡片视图（CARD_VIEW）生效。

- 工具条布局调整为 `[刷新] [🔍 输入框] [搜索] [清空] [spacer] [视图▼]`；
- 搜索匹配：每张卡片的全部可见文本（标题 + 字段名 + 字段值），
  不区分大小写的子串匹配，隐藏不匹配卡片；
- 交互：输入关键词后点「搜索」或按 Enter 触发过滤；点「清空」恢复全部卡片；
- 空结果提示「没有匹配的卡片」，清空后消失；
- 视图联动：卡片视图下搜索控件启用；表格 / 列表视图下禁用（灰显不可交互）并清空搜索词；
- 搜索词仅会话内保留（不持久化到 data.json）：
  切换看板、切到非卡片视图时自动清空；
  store 自动刷新 / 编辑器失焦重跑时过滤自动重新套用，不丢词；
- 实现方式为 DOM 后置过滤，视图层（card-view / table-view / list-view）零改动，
  不影响卡片内联编辑、视图切换、防抖保存等既有功能。

**工程结构重构**：`src/` 迁移为 `dsql / index / ui` 三层目录，功能逻辑零变更。

- `src/query/` → `src/dsql/`（DSQL 语言层，`src/types.ts` 一并移入），保持零 Obsidian 依赖；
- `src/views/` → `src/ui/`（panel / sidebar 移至 ui/ 根，三视图与属性弹窗移至 `ui/views/`），
  `src/utils/viewSync.ts` → `src/ui/utils/viewSync.ts`；`main.ts` / `settings.ts` 留根；
- 跨层引用统一走路径别名 `@dsql/*` / `@index/*` / `@ui/*`
  （tsconfig / esbuild / 测试运行器三处同构配置）；
- 按注释规范重写全部源码注释（文件头 `@module` + `@description`，导出函数 `@param` + `@returns`），
  规范落稿 CONTRIBUTING.md §8；
- 代码清理（删除未使用 import）与文档同步（CONTRIBUTING / API / ARCHITECTURE / README）；
- 验收：`tsc --noEmit` 零错误，106 个单元测试全部通过。

### DSQL 更新

本次无 DSQL 语法 / 语义变更。

- **版本兼容声明**：本版本功能逻辑与 2.1.0 完全兼容。

---

## [示例文件夹更新] - 2026-09-08

### 示例文件夹

**新增跨文件人物关系示例**：补齐跨文件关系与逻辑判断的演示数据。

- 新增 `示例/人物关系示例/`：张三 / 李四 / 王五 / 赵六 / 孙七 五篇人物笔记，
  frontmatter 含 姓名 / 性别 / 年龄 / 阵营 / 身份 / 状态 / **`关系`** 数组，
  正文互相 wiki 链接（`[[...]]`）；
  张三 → 李四、王五、孙七；李四 → 张三、赵六；王五 → 张三；赵六 → 王五、李四；孙七 → 张三、王五；
- 演示库 `test-vault/` 与本地测试库 `test-vault-local/` 同步更新：示例笔记、
  `README.md` 与 `data.json` 看板定义三处一并落地。

**新增「跨文件示例」看板 7 个**（侧栏新分类，看板总数 20 → 27）：

- **人物出链（谁引用了张三）**：`**contains**(file.outlinks, "示例/人物关系示例/张三.md")` → 李四、王五、孙七；
- **人物入链反查（张三的关系圈）**：换用 `file.inlinks` 做反向查询 → 李四、王五、孙七；
- **AND 逻辑判断**：`阵营 %==% '天枢' **AND** 状态 %==% '存活'` → 张三、李四；
- **OR 与优先级（AND 优先于 OR）**：`中立 **OR** 天枢 **AND** 谋士` → 孙七、李四
  （等价于 `中立 OR (天枢 AND 谋士)`）；
- **括号覆盖优先级**：`(天枢 **OR** 幽篁) **AND** 存活` → 张三、李四、赵六（王五已阵亡被剔除）；
- **跨文件 + NOT 组合**：出链命中「王五」`**AND** **NOT**` 阵亡 → 张三、赵六、孙七；
- **人物阵营卡片（CARD_VIEW）**：按「阵营」分列的人物卡，可顺带验收卡片内联编辑 frontmatter。

**新增跨文件夹示例（场景 ↔ 人物）**：两个文件夹之间的双向关系查询。

- 新增 `示例/场景示例/`：长坂坡、赤壁、洛阳商行、幽篁大营 四篇场景笔记，
  frontmatter 含 `场景人物`（数组）/ 类型 / 地点 / 季节 / 结果，正文 wiki 链接到
  `示例/人物关系示例/` 下的人物笔记；
- 与已有的人物文件夹构成跨文件夹关系：场景侧用 `**contains**(场景人物, '张三')` 反查人物，
  人物侧用 `**contains**(file.inlinks, "示例/场景示例/长坂坡.md")` 反查场景。

**新增「跨文件夹示例」看板 7 个**（侧栏新分类，看板总数 27 → 34）：

- **场景检索（含张三的场景）**：`**contains**(场景人物, '张三')` → 赤壁、长坂坡；
- **场景人物清单（跨文件夹引用）**：`场景人物` 数组与 `**length**(file.outlinks)` 双口径对照 → 4 篇场景；
- **人物被某场景引用（入链跨文件夹）**：`file.inlinks` 命中「长坂坡」→ 张三、王五；
- **人物跨场景出场（OR 组合）**：入链命中「长坂坡」`**OR**`「幽篁大营」→ 张三、王五、赵六；
- **场景 + 逻辑（AND / NOT）**：含张三 `**AND** **NOT**` 结果为僵持 → 长坂坡（赤壁被剔除）；
- **出场人次汇总（TOTAL）**：`**length**(场景人物)` 与 `**TOTAL** 1 **AS** $场景数$` 并列 → 4 行；
- **场景卡片（CARD_VIEW）**：按「类型」分列的场景卡 → 4 张。

**演示库 README 同步**：示例数据表新增「人物关系示例」「场景示例」两行与两文件夹对照表；
预置看板 20 → 34、四大类 → 六大类；「可复制示例」段落补齐跨文件与跨文件夹两组示例。

- 测试：14 条新看板 SQL 全部通过 `parseQuery` 解析校验；用含 `关系` / `场景人物` 数组 + outlinks / inlinks
  的模拟行跑通全部 14 条查询，命中结果与上述预期逐条一致。
- **版本兼容声明**：本项仅新增演示数据与看板，不涉及插件功能与 DSQL 语法变更。

---

## [2.1.0] - 2026-09-07

> **项目架构重构（单目录工程化）**

### 插件更新

**工程结构优化**：工程由「正式版 + 开发版」双目录合并为**单目录工程**。

- 源码、测试、脚本、文档与演示库统一放在根目录，插件 ID 统一为 `data-show`。

**构建流程统一**：构建入口、产物路径与同步目标统一。

- `esbuild.config.mjs` 以 `src/main.ts` 为入口、产出 `dist/main.js`；
- 构建完成后自动把 `main.js` / `manifest.json` / `styles.css` 同步到
  `test-vault-local/`（本地测试库）与 `test-vault/`（干净提交库）；
- watch 模式额外监听 `manifest.json` 与 `styles.css` 的变化并自动重建同步。

**测试流程统一**：测试与类型检查的基准路径统一到项目根目录。

- `scripts/test.mjs` 修正为以项目根目录为基准，动态加载 `tests/all.ts` 并执行全部单测；
- `tests/helpers.ts` 的测试库路径常量固定指向 `test-vault-local`；
- `tsconfig.json` 的 `outDir` / `rootDir` 分别指向 `dist` / `src`。

**演示库拆分**：本地验收环境与提交版本分离。

- 本地验收用 `test-vault-local/`（不入库，测试改动不影响提交版本），提交用干净版本 `test-vault/`；
- 两者 `.gitignore` 均排除构建产物、保留 `data.json` 与示例笔记。

**代码清理**：移除冗余导入与残留旧件。

- 删除编译期可证明未使用的导入；
- 统一共享类型出口为 `src/types.ts`；
- 清理测试库中残留的旧开发插件目录（`datashow-dev`），社区插件清单与看板 `viewType`
  统一到 `data-show` / DSQL v2.0 取值。

**文档统一**：按单目录工程重写全部文档。

- 重写 `README.md` / `ARCHITECTURE.md` / `CONTRIBUTING.md` / `API.md`；
- DSQL 规范整理为 `docs/DSQL-语言规范.md`。

**版本与兼容性**：三处版本号统一为 `2.1.0`。

- `manifest.json`、`package.json`、`versions.json` 三处版本号统一为 `2.1.0`；
- 已有 `data.json` 看板定义可直接沿用，无需迁移。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v2.0，权威规范见
[docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)。

- 对已有查询的影响：**兼容**，任何看板 SQL 都无需改动；
- 本版本不涉及 DSQL 语法变更，也不涉及看板视图与界面交互变更。

- 测试：106 例（七套件）全部通过；`npm run build` 通过。
- **版本兼容声明**：本版本功能逻辑与 2.0.x 完全兼容。

---

## [2.0.0] - 2026-09-07

> DSQL v2.0 + 卡片视图 + 字段迁移。
>
> **破坏性大版本**：DSQL 关键词 `TABLE` / `LIST` 直接报语法错误；看板定义 `viewOverride` 字段
> 在加载时迁移到 `viewType`（重命名，语义更明确）。演示库看板已全部同步更新。

### 插件更新

**新增卡片视图（CARD_VIEW）**：Kanban 看板布局。

- 按首个可分组字段（如「状态」）拆列，列头显示彩色圆点 + 分组值 + 数量，列内堆卡片；
- 卡片以文件名作标题（点击打开笔记），**单击字段值直接编辑 frontmatter**
  （回车保存 / Esc 取消 / 失焦还原）；
- 派生列（TOTAL / 表达式 / 变量 / `file.*` / `this.*`）只读；
- 保存走 `app.fileManager.processFrontMatter` 原子写回，索引增量更新自动重跑结果。

**表格 / 列表视图改为只读**：内联编辑统一改为打开笔记。

- 表格移除原双击单元格内联编辑，改为行级 click 打开笔记；
- 列表改为点击文件名 / 行打开笔记，派生列作为缩进子信息（参考 Obsidian Bases 风格）。

**视图切换与 SQL 双向同步**（`src/utils/viewSync.ts`）：下拉切换与 SQL 编辑互为反向同步。

- `applyViewType(board, newType)`：替换 / 注入 SQL 开头视图关键词 + 设置 `board.viewType`；
- `detectTypeFromSql(sql)`：跳过前导空白与 `--` 注释行识别开头 VIEW 关键词（未命中返回 `null`）；
- `normalizeSqlView(sql, type)`：纯函数，强制 SQL 开头关键词与 type 对齐
  （旧 `**TABLE**` / `**LIST**` 一并替换）；
- 面板下拉切换 → 调 `applyViewType` → 保存 → 重渲染；SQL 编辑器防抖保存 → 调
  `detectTypeFromSql` 反向同步下拉选中态（空 SQL / 纯空白不切换，保持当前状态）。

**字段迁移**：`Board.viewOverride` → **`Board.viewType`**。

- 类型 `ViewType | ""`，空 = 跟随语句；
- 加载时按优先级迁移（`viewType` 合法值优先，非法 / 空时 `viewOverride` 兜底）并写回一次；
- `Board.type` 保持 `string` 不变——仍是用户自由填写的看板分类（侧栏分组依据）。

**设置页**：视图模式与看板分类并存。

- 新增「视图模式」下拉（跟随语句 / 表格 / 列表 / 卡片），与「看板分类」自由输入并存；
- 「看板类型」标签改为「看板分类」。

**演示库同步**：看板 18 → 20。

- 新增「任务卡片」「数学卡片」两个 CARD_VIEW 示例；
- 全部看板 SQL 已同步到 DSQL v2.0，`viewOverride` 字段已迁移为 `viewType`；
- 迁移前的看板定义留存为 `data.json.back` 快照，便于回滚比对。

### DSQL 更新

**DSQL 语言升级 v2.0**（规范见 [docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)）：视图关键词统一 `*_VIEW` 后缀。

- 视图关键词 `TABLE` / `LIST` 废除，新增 `TABLE_VIEW` / `LIST_VIEW` / `CARD_VIEW`
  （缺省 `TABLE_VIEW`）；旧 `**TABLE**` / `**LIST**` 直接抛 `LexError`「未知关键词」，不做兼容；**破坏性**
- 三个 `*_VIEW` 关键词全部允许写进 SQL 并持久化到看板定义；
- 执行层：三个视图走完全相同的数据管道，仅渲染不同；
- 字符串字面量 `'**TABLE_VIEW**'` 等不参与视图识别（词法器天然隔离）；
- 对已有查询的影响：**不兼容**——使用旧 `**TABLE**` / `**LIST**` 的查询直接报语法错误，
  须改为 `*_VIEW` 关键词。

- 测试：80 → 106 例（七套件，新增 26 例）；新增 `tests/viewSync.test.ts`（15 例）与
  `tests/normalizeBoard.test.ts`（10 例），`tests/dsql-language.test.ts` 更新 view 字面量
  并新增旧关键词报错与字符串字面量边界用例；`npm run build` 通过。

---

## [1.8.001] - 2026-09-07（插件开发版，已随 1.8.0 迁移）

> DSQL 1.5：三值语义分家 / 别名唯一 / 摄取容错。
>
> 本条目内容已随插件 **1.8.0** 迁移到正式版（插件版本不变，仅 DSQL 语言版本升 v1.5）；
> 演示库的「三值示例」分组与 `data.json` 入库同步记入 1.8.0。

### 插件更新

**调试信息结构化渲染**：warnings 带类型展示。

- warnings 结构化 `{ type, message }`（除零 / 类型不匹配 / 未知函数 / TOTAL / SORT /
  duplicateKey 等），调试页渲染为 `[type] message`。

**测试**：新增摄取层套件。

- 新增 `ingest.test.ts`（重复键 / 摄取归一 4 例）与三值语义、别名唯一性、词法等新例，
  warnings 断言适配结构化，共 **80 例通过**。

### DSQL 更新

**规范升 v1.5**：三处「无」的语义正交分家。

- `0` / `false` 为正常值（仅裸真值为假，`0 %+% 1 = 1` 照常）；
- `null` 为空容器值（摄取归一 `字段: ""` / `字段: []` → null；算术 → null + warning，
  比较 → false）；
- **empty 值**为未赋值（`字段:` 冒号后无内容；除 `empty()` 外一切运算按 null 传播）；
- 实现：EMPTY 哨兵 + 求值层 empty → null 传播，`empty()` 是唯一能看见 empty 值的运算。

**裸真值判断**：假值集合扩大。

- empty 值、null、0、false、空串、空数组均为假（**0 由真改假**）；**破坏性**

**`empty()` 语义收窄**：当且仅当 x 为 empty 值时 true。

- `""` / `[]` / `0` / `false` / null / 缺失字段均 false。

**NUMBER 词法收窄**：小数点后必须至少一位数字。

- `1.` / `.5` 词法报错。

**别名唯一性**（致命错误，聚合遍开始前）：命名空间隔离。

- SELECT 内所有 AS 别名互不相同（解析期校验）；
- 且不得与 FROM 全量命中行字段名并集重复（非 TOTAL 场景同样生效）。**破坏性**

**frontmatter 摄取容错**：重复键隔离而非中断。

- 重复键（自扫描原文顶层键，Obsidian metadataCache 已折叠无此信息）
  → 文件从结果集剔除 + 结构化 warnings `duplicateKey`（含文件名与字段名，原始键值对归档），
  查询继续；
- 受限标注：`字段:` 与 `字段: null` 在缓存层不可区分，
  实现按「键存在但值为 null → empty 值」近似（`字段: null` / `~` 亦映射为 empty）。

**规范文档整理**：小节编号与关键词清单勘误。

- §6 小节编号勘误 5.1–5.8 → 6.1–6.8；删除版本记录重复引言行，「当前」仅标 v1.5；
- §1 关键词清单补 `TOTAL`；新增「摄取容错策略」骨架节。

- 对已有查询的影响：**部分不兼容**——同名别名、依赖 `0` 为真的判断需按新语义调整。

---

## [1.8.0] - 2026-09-07

### 插件更新

**DSQL 1.4 迁移合入**：自测试版 1.7.003 合入 TOTAL 全表聚合与 `$变量$` 派生体系。

- 语言规范升 v1.4，两目录测试与文档版本对齐（详见下方 DSQL 更新）。

**演示库同步**：新增三值示例分组。

- 新增 `示例/三值示例/`（正常零值 / 空容器 / 未赋值 三篇）；
- 预置看板 16 → 18（新增「三值示例」分组：三值展示、empty 谓词与三值传播）；
- 看板定义 `data.json` 已纳入版本控制（含全部 DSQL，视作核心资产）；
- 修复潜在不兼容：原 m4-compare 看板使用「价格 `**AS**` 价格」同名别名，
  在 1.5 别名唯一性规则下会致命报错，已改为「物品价格 / 物品数量」。

**测试补全**：字符串连接与复合表达式 8 例。

- 覆盖字符串连接（null 传播 / 类型转换 / 优先级 / 参与比较）与
  复合表达式（优先级括号 / 逻辑组合 / 函数嵌套）。

**代码清理与文档核对**：删除死代码并统一文档表述。

- 删除死代码 `isEmptyValue`（types）与 `hasFunction`（functions）；
- 文档一致性核对：版本表述、规范链接、调试默认值与源码头部注释版本统一
  （规范 §6.6「调试信息默认开」改为默认关，与 `showDebug: false` 对齐）。

### DSQL 更新

**DSQL 语言升级 v1.5：三值语义分家 / 别名唯一性 / 摄取容错**（插件版本不变，仅语言版本升级）：
把三种「无」的语义正交分离。

- 三种「无」正交定义：`0` / `false` 为**正常值**（仅裸真值判断为假，运算照常）；
  `null` 为**空容器**（`字段: ""` / `字段: []` 摄取为 null）；
  **empty 值**为**未赋值**（`字段:` 冒号后无内容），除 `**empty**()` 外一切运算按 null 传播；
- 裸真值判断：`0` 由真改假（empty / null / 0 / false / 空串 / 空数组均为假）；**破坏性**
- `**empty**(x)` 语义收窄为「仅当 x 未赋值时为真」，`""` / `[]` / `0` / `false` / null 均假；
- NUMBER 词法收窄：`1.` 与 `.5` 为词法错误；
- SELECT 别名唯一性：所有 `**AS**` 别名互不相同，且不得与行字段名冲突（致命错误，
  不限是否含 TOTAL 项）；已有看板若用 `价格 **AS** 价格` 这类同名别名需改别名；**破坏性**
- frontmatter 重复键：该文件从结果集剔除并计入 `duplicateKey` 警告，查询继续；
- warnings 结构化 `{ type, message }`，调试页按 `[type] message` 渲染；
- 对已有查询的影响：**部分不兼容**——同名别名与依赖 `0` 为真的裸真值判断需按新语义调整。

**DSQL 语言升级 v1.4：TOTAL 全表聚合 + $变量$ 派生体系**：引入两遍执行模型。

- 两遍执行模型：聚合遍扫描 FROM 全量命中行（恒忽略 WHERE）计算 `**TOTAL**`；
  投影遍 WHERE / SORT / LIMIT / SELECT，`$变量$` 查变量表、裸标识符查行字段；
- 语法：`**TOTAL** (字段|数字) **AS** 别名`（别名强制）；`$变量$` 仅 SELECT 内可引用；
  不可反向引用、TOTAL 内禁止引用变量；别名与行字段同名 → 致命报错；
- 语义：数值求和（null / 缺失跳过、非数值跳过计入 warnings、空表 → null）；
  `TOTAL 1` = 总行数；仅含 TOTAL 项 → 单行「汇总」结果；派生列只读
  （列表视图以辅助信息行展示）；
- 调试新增 AGG 行。

- 测试：80 例（五套件：功能 22 / 数学 29 / DSQL语言 21 / store 4 / 摄取层 4）。

---

## [1.7.003] - 2026-09-07（插件开发版，已随 1.8.0 迁移）

### 插件更新

**派生列只读**：不可内联编辑。

- 派生变量列不可内联编辑；列表视图以辅助信息行展示。

**调试**：新增聚合调试行。

- debug 新增 `aggregates`（调试页 AGG 行）。

**测试**：47 + 15 例（dsql-language 6 例校验、math 9 例数值语义），共 62 例通过。

### DSQL 更新

**两遍执行模型**：聚合与投影分离。

- 第一遍聚合遍扫描 FROM 全量命中行（恒忽略 WHERE）计算 `**TOTAL**`；
- 第二遍投影遍 WHERE / SORT / LIMIT / SELECT，`$变量$` 查变量表、裸标识符查行字段。

**语法**：`**TOTAL** (字段|数字) **AS** 别名`。

- 别名强制；AS 右侧接受 `$var$` 或裸名，归一化存裸名；
- `$变量$` 仅 SELECT 内可引用（WHERE / SORT 报错）；
- TOTAL 操作数内禁止引用变量；不可反向引用；
- 变量表与行字段命名空间隔离；AS 别名与行字段同名 → 执行期致命报错。

**语义**：数值求和与计数。

- 数值求和（null / 缺失跳过、非数值跳过计入 warnings、全无数值 / 空表 → null）；
- `TOTAL 1` = 总行数（COUNT 等价）；
- SELECT 仅含 TOTAL 项 → 单行合成结果（文件名「汇总」）。

**词法**：新增变量与 TOTAL 词法单元。

- lexer 新增 `$var$` variable token 与 TOTAL 关键词。

**规范**：`docs/DSQL-EBNF.md` 升 v1.4。

- 补充变量表查询规则 / 别名冲突规则 / TOTAL 计数 / 过滤后聚合变通方案
  （现归档于 [docs/DSQL-语言规范.md](docs/DSQL-语言规范.md)）。

---

## [1.7.002] - 2026-09-07（插件开发版，已随 1.7.0 迁移）

> 移动端调试信息改标签页

### 插件更新

**结果区改为标签页**：调试信息独立滚动。

- 「查询结果 / 调试信息」两个标签切换——原 `<details>` 折叠面板
  在移动端跟随正文长列表无法单独滚动，现调试列表限高 45vh 独立滚动。

**`showDebug` 默认值改为关闭**：已保存过设置的用户不受影响。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3（DSQL v1.4 由后续 1.7.003 引入）。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.7.001] - 2026-09-07（插件开发版，两目录对齐清理）

### 插件更新

**清理**：移除未实装视图类型的死代码。

- 移除 `PLANNED_VIEWS`（board / card / calendar / stats）及其下拉灰显项
  （该死代码仅存在于测试版，正式版本就未包含，清理后两目录对齐）；
- 未实现功能的规划统一记录在工作区根目录 `TODO` 文件（当前留空）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.7.0] - 2026-09-07

### 插件更新

**最低版本要求提升为 Obsidian 1.4.4**：修正官方 API 版本声明。

- 属性编辑依赖的 `fileManager.processFrontMatter` 官方标注 @since 1.4.4，
  原 1.4.0 声明在 1.4.0–1.4.3 上会运行报错。

**稳定性修缮**：异步与索引重建收敛。

- `workspace.revealLeaf` 全部改为 `await`（该 API 自 1.7.2 返回 Promise，
  确保视图完全加载、避免 deferred leaf 未就绪）；
- `metadataCache.on("resolved")` 改为仅首次全量重建，之后走增量路径
  （该事件在启动后每次批量修改解析完成都会触发，原先每次都全量扫描）。

**写法现代化**：以下写法均为官方推荐形式。

- `vault.getAbstractFileByPath` → `getFileByPath`；
- `workspace.getLeaf(true)` → `getLeaf('tab')`。

**结果区改为标签页**：调试信息独立滚动。

- 「查询结果 / 调试信息」两个标签切换——原 `<details>` 折叠面板
  在移动端跟随正文长列表无法单独滚动，现调试列表限高 45vh 独立滚动。

**「显示 DSQL 调试信息」默认改为关闭**：已保存过设置的用户不受影响。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.6.001] - 2026-09-07（插件开发版，已随 1.7.0 迁移）

### 插件更新

**minAppVersion 1.4.0 → 1.4.4**：修正官方 API 版本声明。

- 属性编辑依赖的 `fileManager.processFrontMatter` 官方标注 @since 1.4.4，
  原声明在 1.4.0–1.4.3 上会运行报错。

**`workspace.revealLeaf` 全部改为 `await`**：确保视图完全加载。

- 该 API 自 1.7.2 返回 Promise，官方要求 await 以确保视图完全加载（避免 deferred leaf 未就绪）。

**`metadataCache.on("resolved")` 仅首次全量重建**：避免重复全量扫描。

- 该事件在启动后每次批量修改解析完成都会再触发，原先每次都全量扫描；
  之后统一走增量路径（flush 已重算反向链接）。

**现代化写法统一**：替换为官方推荐形式。

- `vault.getAbstractFileByPath` → `getFileByPath`（带类型返回值）；
- `workspace.getLeaf(true)` → `getLeaf('tab')`（字符串形式为推荐写法）。

**已知不修**：`processFrontMatter` 保存会规范化重写整个 YAML（官方行为，属性弹窗文案提示即可）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [工程调整] - 2026-09-07（未升版本，随 1.6.0）

### 插件更新

**测试按示例三大类重组**：拆分原 `sql.test.ts`。

- `tests/sql.test.ts` 拆分为 `feature.test.ts`（功能示例 22 例）、
  `math.test.ts`（数学示例 9 例）、`dsql-language.test.ts`（DSQL语言示例 12 例），
  共享数据提取到 `helpers.ts`；
- 连同 `store.test.ts` 共 47 例全部通过。

**演示库各自独立**：两库内容一致但只部署本工程插件。

- 两个测试库内容一致（示例数据与预置看板按 功能示例 / 数学示例 / DSQL语言示例 三大类组织），
  但各自只部署本工程插件（曾尝试共用同一 vault，实测报错，已回退）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.6.0] - 2026-09-07

### 插件更新

**frontmatter 属性编辑**：视图内直接改属性并自动刷新。

- 表格视图双击单元格直接改属性（回车保存，自动转型）；
- 列表视图点击条目或行内 ✎ 打开 YAML 属性编辑弹窗（官方 processFrontMatter 写回）；
- 保存后索引增量更新、查询结果自动刷新。

### DSQL 更新

**DSQL 语言规范升级 v1.3**：空优先级与自动列排序的行为明确。

- 空 `**BY** ()` 视为无自定义优先级并计入警告；
- SELECT `*` 自动列按 UTF-8 字节序排序；
- 排序方向语义文档勘误（末尾方向属于最后一个键）。

---

## [1.4.001] - 2026-09-07（插件开发版，已随 1.6.0 迁移）

### 插件更新

**frontmatter 属性编辑**（看板数据直接改，保存后索引增量更新、查询结果自动刷新）：
全链路走官方 API。

- 表格视图：双击可编辑单元格（直接 frontmatter 字段）→ 输入框 → 回车保存
  （Esc 取消、失焦还原）；空值 → null，true / false 字面量、数字自动转型，其余存为字符串；
  数组列不可内联编辑；
- 列表视图：点击条目打开属性编辑弹窗；表格行悬停出现 ✎ 铅笔，点击同样打开；
- 属性弹窗：官方 API 全链路——`metadataCache` 读 → `stringifyYaml` 展示 →
  `parseYaml` 校验（失败显示错误不写入）→ `fileManager.processFrontMatter` 原子写回；
  Ctrl / Cmd + Enter 快捷保存；
- 官方 API 核对：`processFrontMatter`（obsidian.d.ts:2954）、`parseYaml`（:4817）、
  `stringifyYaml`（:6815）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.3。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [DSQL 1.3.001] - 2026-09-07（语言版本修订，插件仍为 1.4.0）

### 插件更新

**调试 `from` 描述明确**：口径写清。

- 明确为「源解析后的命中行数（去重后）」。

### DSQL 更新

**评审项落地（H1–H3 / M1–M3 / L1–L4）**：EBNF 勘误与语义补全。

- EBNF 勘误：`OP_ADD` 多余空格；优先级表 `%||%` 管道转义；
  `sort_item` 文法改为 `{ sort_modifier }`（方向与 **BY** 各至多一次、先后不限，与实现一致）；
- 语义补全：`comparison` 裸操作数真值语义正式写入；键级方向继承子句级方向、均无默认 **ASC**；
  数字不支持科学计数法与负数字面量（词法即如此，补文档）；
- 行为微调（含实现改动）：空 `**BY** ()` 视为无自定义优先级并计入 warnings；
  SELECT `*` 自动列排序改用 UTF-8 字节序（原为 JS 默认序，增补平原字符集外更严谨）；
- 对已有查询的影响：**兼容**（仅空 `**BY** ()` 会新增一条 warning）；
  对应的语言规范章节同步更新（排序方向、真值语义、词法边界）；
- 规范 §7 新增示例：键级方向 + 子句级方向 + 多个 custom_order 组合。

- 测试：41 + 4 例通过（新增空 BY 警告、自动列字节序 2 例）。

---

## [1.4.0] - 2026-09-07

### 插件更新

**设置与视图下拉**：新增小数位数设置，收敛视图选项。

- 设置新增「小数显示位数」（非负整数，默认 4，仅显示层）；
- 视图下拉仅保留已实装的表格 / 列表。

**新增效果验收 vault**：构建自动同步插件产物。

- 新增 `test-vault/`（构建自动同步插件产物）。

**演示数据扩充**：看板即点即看。

- `data.json` 预置 12 个看板（任务 2 / 演示 6 / 跨项目 4）；
- 演示库扩充为 Projects/Alpha、Projects/Beta、Archive/2025、Inbox 多项目结构，
  任务笔记补互链（outlinks 反查可出结果）与日期 / 布尔 / 数组字段。

**调试信息合并**：字段缺失提示并入投影期。

- 查询结果调试信息并入 SELECT 投影期字段缺失提示（面板收集后与执行期合并去重）。

- 测试：39 + 4 例（新增子句语义 6 例、跨目录多组数据 6 例）。

### DSQL 更新

**DSQL 子句改为按前件关系解析**：书写顺序自由。

- 修复面板报「预期 `**SELECT**`，实际为 `**FROM**`」：每条子句至多一次，
  `**FROM**` 成为唯一必填子句，`**WHERE**` / `**SORT**` / `**LIMIT**` 以 `**FROM**` 为前件；
- `**SELECT**` 可省略（默认全字段）；
- `**WITHOUT** **ID**` 可写在任意子句位置，子句重复出现时报错（带行列号）；
  前件缺失报「需要 `**FROM**` 作为前件」，同样带行列号。

---

## [1.2.0] - 2026-09-07

### 插件更新

**看板数据统一存插件 data.json**：移除未使用的 DataShow 目录约定。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.1.0] - 2026-09-07

### 插件更新

**列表视图与自动列**：查询支持省略 `**SELECT**`。

- 查询支持省略 SELECT 的列表视图与全字段自动列；
- 测试数据与示例看板完善。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.0.1] - 2026-09-07

### 插件更新

**移除旧版兼容**：迁移函数替换为纯校形。

- 删除 `migrateBoard`（sections.data → sql 的开发期数据迁移），
  替换为纯字段校形 `normalizeBoard`；
- v1.1 时代遗留数据不再做任何回退转换。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：语言版本仍为 v1.2。

- 对已有查询的影响：**兼容**，无需改动任何看板 SQL。

---

## [1.0.0] - 2026-09-07

### 插件更新

**首个正式版本**：看板、索引与视图齐备。

- 看板（名称 / 类型 / 说明 / DSQL）、看板侧栏与面板；
- metadataCache 增量索引；
- 表格 / 列表视图；
- 正式版附 `versions.json`、干净的用户 README 与 DSQL 规范文档；
- 工作区拆分为 `data_show/`（正式版，插件 id `data-show`）与 `data_show_test/`
  （测试版，插件 id `datashow-dev`），二者可在同一 vault 共存；
- 正式版清理开发痕迹：统一文件头、移除测试 / 方案 / test-vault 等开发产物。

### DSQL 更新

**DSQL v1.2 标记语法**：作为首个正式版本的语言基线。

- 关键词 / 函数 `**WORD**` 包裹、运算符 `%op%` 包裹、字符串单引号、路径双引号；
- 对已有查询的影响：作为初始版本，无历史查询需要兼容。

---

## [0.8.0] - 2026-09-07

### 插件更新

**调试扩展**：新增结构化告警与源统计。

- 调试扩展 `warnings`（非致命问题 + 次数）与 `sourceStats`（逐叶子源行数），
  补充 where 剔除示例（≤3）与 sort 比较次数；解析错误统一 `[DSQL]` 前缀；
- 测试 29 + 4 例。

### DSQL 更新

**v1.2 修订 2**：优先级、大小写与边界语义明确。

- 数据源 **AND 优先于 OR**（两级文法，括号可覆盖）；
- `**BY**` 优先级改为作用于其书写的排序键；
- `**contains**` 改为**区分大小写**（数组严格 `===`，字符串子串），
  忽略大小写用 `**contains**(**lower**(字段), '值')`；
- 乘方结果非有限数 → null；
- null 比较语义明确（参与比较 → false，`%!=%` 取反，`null == null` → true）；
- 对已有查询的影响：**部分不兼容**（AND / OR 优先级、`contains` 大小写敏感性的结果可能变化）。

---

## [0.7.0] - 2026-09-07

### 插件更新

**测试重写**：25 + 4 例。

### DSQL 更新

**DSQL v1.2 大版本（标记语法字面化）**：语法全面标记化。**破坏性**

- 关键词 / 函数 `**WORD**` 包裹、运算符 `%op%` 包裹、字符串单引号、路径双引号；
- 未知 `**WORD**` 词法报错；`**SELECT**` 成为必须子句，
  视图 `TABLE` / `LIST` 缺省 `TABLE`，`*` 全字段；
- 投影支持任意表达式 + `**AS**` 别名（无别名时：字段用路径名、表达式为「列N」）；
- 完整表达式（乘方右结合、取模、连接 `%||%`、比较族、一元正负）；
- 非致命语义问题（类型不匹配 / 除零 / 缺字段）→ null；
  调试对象 `{ from, where, sort, limit, fieldMisses, executionTimeMs }`；
- 内置函数新增 sqrt / cbrt / root(x, n)；
- 多级排序（键级方向）；
- 对已有查询的影响：**不兼容**——所有查询须改用标记语法书写。

---

## [0.6.0] - 2026-09-07

> SORT BY 优先级 + 调试信息

### 插件更新

**调试信息与设置开关**：测试 32 + 4 例。

- 执行期调试收集（FROM 命中 / WHERE 过滤 / SORT 详情 / LIMIT 截断 / FIELD 字段缺失）；
- 设置新增「显示 DSQL 调试信息」开关（默认开，面板折叠展示；1.7.0/1.7.002 起改默认关）。

### DSQL 更新

**`**SORT**` 字段 `**BY**` 自定义优先级**。

- `**SORT** 字段 **BY** ('值1','值2',...)` 自定义优先级（此版本作用于首键，
  0.8.0 起改为作用于其书写的排序键）；
- 规范文档升 v1.1。

---

## [0.5.0] - 2026-09-07

> 排序规范化 + 测试补全 + EBNF

### 插件更新

**测试与文档**：测试扩至 DSQL 25 例 + store 4 例；新增 DSQL 规范文档 v1。

- 期间修正解析器「多余子句静默忽略」的缺陷，并补 EOF 校验。

### DSQL 更新

**排序规范化**：`**SORT**` 改 UTF-8 字节序确定性排序。

- 逐字节比较、公共前缀递归下降、短者在前；数字按数值、null 恒排末尾；
- `=` / `!=` 定义为字节精确匹配（区分大小写）。

---

## [0.4.0] - 2026-09-07

> 面板编辑化（DSQL 定名）

### 插件更新

**面板编辑化**：DSQL 编辑器移入看板面板。

- 看板模型精简为 `名称 + 类型 + 说明 + SQL`，模板 / 规则彻底移除；
- DSQL 编辑器移入看板面板（等宽输入、防抖 500ms 自动保存）；
- 工具条：刷新按钮（手动重跑）+ 视图类型下拉（表格 / 列表实装，可覆盖语句类型并持久化；
  看板 / 卡片 / 日历 / 统计标为规划中项，1.7.001 已清理）；结果区独立刷新不打断编辑。

### DSQL 更新

**查询语言正式定名 DSQL**（DataShow Query Language）。

---

## [0.3.0] - 2026-09-07

> 索引层 + DSQL 前身打通

### 插件更新

**索引层打通**：全量首扫 + 增量监听。

- 看板组成收敛为**数据 / 模板 / 规则**（关系由模板表达），
  SQL 从散落各笔记改为插件集中管理；
- 索引层 metadataCache 全量首扫 + `changed/deleted/renamed` 增量监听（300ms debounce）；
- 行仓库（笔记 = 行，frontmatter = 列，`file.*` 虚拟列含出 / 入链），订阅通知。

**面板新增「查询结果」区**：表格 / 列表渲染、文件名点击跳转、索引变化自动重跑。

- 测试：store 4 例。

### DSQL 更新

**自研 SQL v1 子集**：DSQL 前身。

- 语法 `TABLE|LIST ... FROM 路径/#标签 [OR|AND] WHERE 表达式 SORT LIMIT`；
- 内置 contains / length / lower / upper / empty，支持中文（Unicode）标识符，错误带行列号；
- 测试：10 例；DSQL 语法参考写入演示库 README。

---

## [0.2.0] - 2026-09-06

### 插件更新

**看板模型引入**：看板改为在插件设置中定义。

- 看板改为在**插件设置**中定义（名称 / 类型 / 作用描述）；
- 首次安装自带空白「默认看板」；
- 侧栏改为展示设置中的看板（按类型分组），点击在主工作区打开；
  `DataShow` 目录此时仅作数据约定（1.2.0 起移除）。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：查询语言尚未定名。

---

## [0.1.0] - 2026-09-06

### 插件更新

**插件骨架**：调研与脚手架。

- 前期调研 Dataview / Datacore / Breadcrumbs 源码与维护现状，产出架构方案
  （五层单向依赖 + ADR 决策记录；方案文档 `datashow方案.md` 后续已废弃删除）；
- P1 脚手架（manifest / esbuild / tsconfig / test-vault 热更新）；
- 插件设置页（常规项）；
- 侧栏树（`DataShow` 目录下 .md 即面板、子目录即分组，事件自动刷新）
  与主区面板视图骨架（board 视图骨架 + 正文 Markdown 渲染）；
- ribbon 图标与命令。

### DSQL 更新

**本次无 DSQL 语法 / 语义变更**：查询语言尚未定名。
